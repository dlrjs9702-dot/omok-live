# Game Center 인수인계

이 문서는 ChatGPT와 Claude가 교대로 작업할 때의 공통 기준입니다. 대화 기록보다 GitHub `main`과 이 문서를 우선합니다. 세션이 끊기면 다음 작업자는 대화 맥락 없이 이 문서 + `main`의 실제 코드 + `lib/release-announcements.js` 이력만 보고 이어받을 수 있어야 합니다.

## 현재 상태

- 저장소: `dlrjs9702-dot/omok-live`
- 기준 브랜치: `main`
- 현재 릴리스: `v1.6.43`
- 이전 운영 커밋: `06aca72` (v1.6.42)
- 최신 반영 커밋: `(이 릴리스의 커밋 해시로 다음 세션에서 갱신)` (v1.6.43)
- 배포 서버(권위 있는 실행 URL): `https://omok-live.onrender.com` — Render 서비스 `omok-live`(`srv-dakk44afngtc73ano4f0`), `main` 브랜치 커밋 시 자동 배포(`autoDeploy: commit`)
- 참고: 과거 문서에 있던 Cloudflare Workers 프록시 주소(`silent-lake-9bcf.dlrjs9702.workers.dev`)는 이번 세션에서 재확인하지 않았다. 접속 안 되면 Render 주소를 기준으로 삼는다.

## 게임과 공통 기능

현재 등록된 게임(코드 기준, `server.js` health 응답의 `games` 목록과 동일): 오목(omok), 오목 2vs2(omok2v2), 사목/커넥트4(connect4), 오델로(othello), 윷놀이(yut), 빙고(bingo), 점과 상자(dots), 랜드킹(cityking), 숫자야구(baseball), 그림 맞히기(pictionary), 라이어게임(liar), 도둑잡기(oldmaid) — 총 12종.

공개·비공개방, 방 비밀번호, 대전 초대, 관전, 재접속, 재대결, 로비·방 채팅, 입장 파일(게스트 키), 관리자 입장 파일 관리·메모·공지사항·접속 현황, 전체 게임 누적 전적 및 1:1 상대 전적 조회를 공통 기능으로 유지한다.

## 릴리스 이력 (v1.6.24 ~ v1.6.43 요약)

전체 공지 문구는 `lib/release-announcements.js`에 릴리스별로 저장되어 있다(코드가 원본, 아래는 목차용 요약).

- v1.6.24 라이어게임 추가
- v1.6.25 도둑잡기(Old Maid) 추가
- v1.6.26 공지사항 버전순 정렬 수정
- v1.6.27 게임방 채팅 높이·스크롤 개선
- v1.6.28 오목 선택 통합, 랜드킹 UI 개선
- v1.6.29 랜드킹 도시 건설, 접속 종료 확인, 윷놀이 경로 수정
- v1.6.30 전체 게임 누적 전적 및 플레이어 조회(영구 저장)
- v1.6.31 랜드킹 더블·전체 턴 계산 수정
- v1.6.32 4자리 숫자야구 진행 불가 버그 수정
- v1.6.33 윷놀이 중앙 경로/빽도/완주 규칙, 1:1 상대 전적, 도둑잡기 UI 다크테마 정리
- v1.6.34 도둑잡기 카드 테이블 UI 및 연출 강화
- v1.6.35 게임방 채팅 전면 개편, 랜드킹 시각화·매각·파산·4인전 확장, 도둑잡기 특수 능력 모드
- v1.6.36 채팅·랜드킹·도둑잡기 사용성/안정화 보완, 공통 주사위 굴림 애니메이션
- v1.6.37 랜드킹 게임판 7×11 확장 및 중앙 조작 패널, 3D 큐브 주사위
- v1.6.38 윷놀이 던지기 애니메이션(고급 CSS/JS 연출)
- v1.6.39 랜드킹 주사위 연출을 윷놀이 던지기와 같은 방식으로 보강
- v1.6.40 접속 끊김 공통 처리(1/5) — 모든 게임 공통 일시정지·게임 종료·전적 기록
- v1.6.41 윷놀이 UI 개선(2/5) — 중앙 텍스트 정리, 출발칸 파란색, 이동 버튼 칸수 표기
- v1.6.42 라이어게임 힌트 중 채팅 제한(3/5)
- **v1.6.43 (현재) 도둑잡기 카드 UI·연출 개선(4/5)** — 아래 상세

v1.6.23 이전(v1.6.7~v1.6.16 공지 복원 포함)의 세부 이력은 `lib/release-announcements.js`의 git 이력 및 해당 파일 내 각 항목 본문을 참고한다. 이 문서에서는 별도로 중복 기술하지 않는다.

## v1.6.43 확정 범위 (최신 작업 상세)

"Game Center 통합 패치 작업 지시서" 5개 항목 중 **4번 항목(도둑잡기 카드 UI 및 애니메이션)**만 포함한다. 5번(신규 게임 마라톤)은 v1.6.34/v1.6.35에서 이미 구현된 카드 테이블·뽑기 애니메이션·짝 완성 연출·특수 능력 모드를 그대로 두고, 지시서에서 실제로 남아 있던 부분만 패치했다(중복 구현 없음).

- **카드 뒷면**: `.oldmaidBack`이 `🎴` 이모지 텍스트를 렌더링하던 것을 없애고, `repeating-linear-gradient` 대각선 줄무늬 + 안쪽 테두리 프레임만으로 구성된 순수 CSS 카드 뒷면으로 바꿨다. 기기/브라우저의 이모지 폰트에 의존하지 않으므로 어디서나 동일하게 보인다.
- **카드 확대 · 터치 영역**: 상대방 카드를 데스크톱 `clamp(26,40)→clamp(34,50)px`, 모바일 `clamp(22,32)→clamp(36,46)px`로 키우고, 카드 겹침 폭을 데스크톱 `-15px→-9px`, 모바일 `-12px→-7px`로 줄였다. 기존 마진 값에서는 카드 3~4장만 겹쳐도 이전 카드의 상당 부분이 다음 카드에 가려져 탭이 안 됐는데, 카드가 커지고 겹침이 줄어 각 카드의 클릭 가능 영역이 넓어졌다. 기존 가로 스크롤(`overflow-x:auto`)과 반응형 구조는 그대로 유지했다.
- **뽑기 애니메이션**: 기존에는 클릭 즉시(서버 응답 전에) 카드 뒷면 그림이 날아가는 낙관적 연출이었고, 얼굴은 전혀 보여주지 않았다. `oldmaidDrawCard()`를 다시 짜서 `await roomAction('draw-oldmaid', ...)`가 끝난 뒤 내 손패를 요청 전/후로 비교(`beforeIds`/`afterHand`)해 실제로 새로 들어온 카드를 찾고, 그 카드가 있을 때만(즉 요청이 실제로 성공했을 때만) `oldmaidFlyDrawnCard()`가 그 카드의 실제 무늬·숫자를 얼굴이 보이는 상태로 클릭한 상대 카드 위치에서 내 손패까지 날아가게 했다. 이 요소는 뽑은 사람의 브라우저 안에서만 만들어지므로 다른 참가자·관전자 화면에는 애초에 나타나지 않는다.
- **짝 제거 애니메이션**: 같은 방식으로 요청 전/후 손패 차이를 비교해 방금 제거된 카드(보통 2장)를 찾아 `oldmaidFlyPairsToDiscard()`로 손패 중앙을 향해 모이다가(`translateX`) 노란 글로우(`box-shadow`)가 잠깐 켜지고 줄어들며 사라지는 연출을 추가했다. 다른 참가자가 짝을 완성했을 때는 카드 내용을 알 수 없으므로 기존처럼 좌석 펄스 + "PAIR!" 배지만 표시된다(정보 누출 없음). 두 경우 모두 실제 `state.me.myOldMaidHand` 배열은 건드리지 않는 장식용 복제 요소라서, 서버가 이미 제거한 카드를 클라이언트가 다시 손패에 추가하는 일은 없다.
- **안정성**: 중복 선택 차단(`oldmaidDrawBusy`), 애니메이션 중 중복 요청 차단(모든 `.oldmaidBack` 비활성화)은 기존 그대로 유지된다. 연출은 실패한/오래된 요청에서는 아예 실행되지 않고(손패에 새 카드가 없으므로), 재접속 시 카드 상태는 항상 서버가 다시 보내주는 `state.me.myOldMaidHand`로 복구된다(변경 없음). `oldmaidEffectsActive()`(효과 끄기 토글 + `prefers-reduced-motion`)를 그대로 재사용해 새 연출 두 개 모두 게이팅했다. 조커·특수 능력 규칙은 전혀 건드리지 않았다.
- 테스트: `test/oldmaid-ui.test.js`의 낡은 이모지/낙관적-애니메이션 단언 4개를 새 구현에 맞게 갱신하고, 서버-확정 순서·짝 컨버전스가 실제 손패를 건드리지 않는지 확인하는 테스트 2개를 추가했다. `npm test` 전체 227개 통과. Playwright로 2인 도둑잡기 방을 만들어 카드 뒷면이 이모지 없이 CSS 무늬로 렌더링되는 것과 카드 크기가 커진 것을 스크린샷으로, 실제 뽑기 진행 중 콘솔 오류가 없는 것을 라이브로 확인했다(짝 완성은 카드 분배가 무작위라 이번 확인에서 직접 재현하지는 못했고, 코드 경로는 위 신규 테스트로 검증했다).

## v1.6.42 확정 범위

"Game Center 통합 패치 작업 지시서" 5개 항목 중 **3번 항목(라이어게임 채팅 제한)**만 포함한다. 4~5번(도둑잡기 카드 UI/애니메이션, 신규 게임 마라톤)은 다음 세션에서 이어간다.

- 서버: `/api/room/chat` 핸들러 맨 앞에 `isLiar(room) && room.game.status === 'playing' && ['hint1','hint2','extraHint'].includes(room.game.phase)` 조건을 추가해, 힌트 단계(1차·2차·동률 후보 추가 힌트)에는 어떤 클라이언트로도 우회할 수 없게 `409 LIAR_HINT_CHAT_LOCKED`로 거부한다. 다른 게임과 라이어게임의 나머지 단계(투표·재투표·최종 추측·결과 공개·자리 선택)는 전혀 건드리지 않았다.
- 클라이언트: `chatLockedForHints()`가 `isLiarGame()`으로 게임 종류를 먼저 확인하므로 다른 11개 게임의 채팅에는 영향이 없다. 잠기면 `chatInput`/`chatSendBtn`을 비활성화하고, 채팅창 위에 "힌트 진행 중에는 채팅할 수 없습니다." 안내(`#chatLockNotice`)를 표시한다. `sendChat()`에도 동일 조건의 조기 반환을 추가해 이중으로 막았다. 힌트 단계가 끝나면(투표 등으로 넘어가면) 같은 렌더 사이클에서 자동으로 다시 활성화된다 — 별도 처리 불필요.
- 테스트: `test/liar-server.test.js`(실제 서버 기동 — 힌트 진행 중 채팅 409, 힌트 6회 모두 차단 확인, 투표 단계 진입 후 채팅 200 확인), `test/liar.test.js`(신규, 클라이언트 소스 코드에 잠금 로직·안내문·버튼 비활성화가 존재하는지 검사) 모두 통과. `npm test` 전체 225개 통과. Playwright로 3인 라이어게임 방을 만들어 힌트 단계 진입 → 채팅 입력창/전송 버튼 비활성화 + 안내문구 노출을 라이브로 확인했고, 같은 상태에서 서버 API로 직접 채팅을 시도해 `409 LIAR_HINT_CHAT_LOCKED`가 반환되는 것도 함께 확인했다.

## v1.6.41 확정 범위

"Game Center 통합 패치 작업 지시서" 5개 항목 중 **2번 항목(윷놀이 UI 개선)**만 포함한다. 3~5번(라이어게임 채팅 제한, 도둑잡기 카드 UI/애니메이션, 신규 게임 마라톤)은 다음 세션에서 순서대로 이어간다. 이동·업기·잡기·빽도·완주 등 실제 게임 로직은 전혀 건드리지 않았다(순수 UI 변경).

- 게임판 중앙에 떠 있던 "지름길"(중앙 교차점 위) · "출발 · 완주"(출발 칸 옆) 텍스트 라벨을 제거했다(`drawYutBoard()`). 경로 선·교차점 원은 그대로 유지했고, "완주" 수치는 이미 보드 위 점수 줄(`gameScoreText`: "파랑 완주 N · 빨강 완주 N")과 보드 하단 범례("파랑 집 N · 완주 N")에 남아 있어 정보 손실은 없다.
- 출발 칸(0번 노드)은 다른 칸과 같은 갈색 링 스타일 대신 반지름 30의 꽉 찬 파란색(`#1d4ed8`) 원 + 연노랑 테두리로 그려 한눈에 구분되도록 했다. 파랑·빨강 말이 그 위에 서도 말의 흰 테두리·흰 숫자 덕분에 대비가 유지된다.
- 말 이동 선택 버튼 문구를 도착 칸 번호 중심("1번 말 → 24번 칸")에서 이동 칸수 중심으로 바꿨다: 일반 이동은 "1번 말 · 3칸 이동", 빽도는 "1번 말 · 1칸 뒤로", 업힌 말은 "1·2번 말 · 3칸 이동"처럼 표시하고 완주/완주 직전 칸이면 " · 완주"/" · 완주 직전 칸"을 덧붙인다. `roomAction('move-yut', ...)` 호출과 `legalMoves` 자체는 손대지 않았다.
- 테스트: `test/yut.test.js`에 새 텍스트·출발칸 스타일·제거된 라벨을 확인하는 테스트를 추가했고, 기존 업힌 말 텍스트 검증도 새 포맷에 맞춰 갱신했다. `npm test` 전체 224개 통과. Playwright로 윷놀이 방을 만들어 실제 이동 버튼 문구("1번 말 · 3칸 이동")와 보드 스크린샷(출발 칸이 파란 원으로, 중앙/출발 텍스트 없이 렌더링됨)을 1회 확인했다.

## v1.6.40 확정 범위

사용자가 5개 항목으로 구성된 "Game Center 통합 패치 작업 지시서"를 요청했고, 순서대로 하나씩 구현·검증·배포하기로 했다. 이번 릴리스는 그중 **1번 항목(공통 접속 끊김 처리 및 승패 기록 개선)만** 포함한다. 2~5번(윷놀이 UI, 라이어게임 채팅 제한, 도둑잡기 카드 UI/애니메이션, 신규 게임 마라톤)은 다음 세션에서 순서대로 이어간다.

- 기존에는 오목 2vs2 팀전에만 있던 "일시정지 → 종료" 메커니즘(`syncTeamPause`)을 `syncGamePause`로 일반화해 12개 게임 전체(2인 게임·3인 이상 자유경쟁 게임·4인 팀전 모두)에 적용했다. `assignedSeatsFor(room)`로 게임 유형에 관계없이 배정된 좌석을 구하고, 그중 연결이 끊긴 좌석이 있으면 `game.paused = true`로 표시한다.
- `end-game` 액션을 다시 작성했다: 더 이상 방장 전용·팀 전용이 아니며, **접속 중인 좌석에 앉은 참가자라면 누구나** 일시정지 상태에서 즉시 대국을 종료할 수 있다(관전자는 불가, 일시정지 상태가 아니면 `NOT_PAUSED`로 거부). 승패 기록 규칙은 사용자가 명시적으로 확정한 대로 구현했다: 접속 중인 참가자 전원 승리, 접속이 끊긴 참가자 패배(2vs2 오목 팀전은 팀 단위로 처리 — 팀원 한 명만 끊겨도 그 팀 전체 패배). 3인 이상 자유경쟁 게임(빙고 등)에서 여러 명이 접속 중이면 그 전원이 승리로 기록된다. `game.winner`는 각 게임 엔진이 원래 쓰던 모양(단일 값 vs 배열)을 그대로 따르므로 `buildMatchResult()`는 수정하지 않았다.
- 결과는 `room.recordedMatches`의 기존 라운드 단위 중복 방지 로직을 그대로 타므로 별도 처리 없이 한 번만 기록된다. 새 `game.endReason`(`'disconnect'`)·`game.disconnectedAtEnd` 필드로 "접속 끊김으로 종료된 결과"를 구분해서 표시하며, `prepareNextRound()`에서 두 필드 모두 초기화해 다음 판에 orphan 데이터가 남지 않는다.
- 클라이언트: 모든 게임 공통으로 참가자(관전자 제외)에게 "참가자 OOO님의 연결이 끊겨 게임이 일시 중단되었습니다" 팝업(`<dialog id="pauseDialog">`)을 띄운다. 순간적인 네트워크 지연이 팝업을 깜빡이게 하지 않도록 2초 디바운스를 두었고(그 사이 재접속하면 아예 뜨지 않음), "기다리기"는 그 disconnect 사건 하나만 닫고 "게임 종료" 버튼(`endGameBtn`/`sideEndGameBtn`)은 계속 화면에 남아 있다. 상태 표시줄·보드 오버레이의 일시정지·종료 문구도 팀 전용 조건을 없애고 전체 게임에 적용했다.
- **의도적으로 이번 범위에서 제외한 것**: `move`(오목·오델로·사목·점과 상자·팀전 오목) 액션에는 일시정지 차단을 추가했지만, 그 외 약 15개의 게임별 액션 핸들러(랜드킹 주사위 굴리기, 윷 던지기, 도둑잡기 카드 뽑기, 라이어 힌트/투표/추측, 그림 맞히기 획/정답, 빙고 숫자 선택, 숫자야구 추측 등)에는 일시정지 차단을 개별적으로 추가하지 않았다. 이유: "이 특정 좌석의 접속 끊김이 지금 진행을 실제로 막는가"는 게임마다 판단 기준이 달라 훨씬 큰 작업이 되고, 핵심 요구사항(감지·기다리기/종료 선택·정확한 승패 기록)은 공유 `move` 핸들러 하나만 일반화해도 충족되기 때문이다. 실제로 막힌 게임 진행은 없다 — 접속이 끊긴 좌석이 자기 차례가 아니면 어차피 다른 참가자가 계속 둘 수 있고, 종료 옵션은 모든 게임에서 항상 열려 있다.
- 테스트: `test/connection-drop.test.js`(신규, 2인 게임과 3인 이상 자유경쟁 게임의 실제 서버 기동 통합 테스트), `test/omok-team.test.js`(팀 단위 일시정지·종료 시나리오 갱신), `test/connection-drop-ui.test.js`(신규, 팝업·종료 버튼·문구가 전체 게임 공통으로 존재하는지 소스 검사) 모두 통과. `npm test` 전체 223개 통과. Playwright로 오델로 2인 방에서 실제 연결 끊김 → 팝업 표시(2초 디바운스 확인) → 팝업 안 "게임 종료" 클릭 → 상태 표시줄/보드 오버레이에 "접속 끊김으로 종료" 문구까지 라이브로 1회 확인했다(전수 브라우저 회귀는 수행하지 않음).

## v1.6.39 확정 범위

사용자가 "랜드킹 주사위도 윷 던지기와 같은 방식으로 업데이트해줘"라고 요청해, 공통 `animateDiceRoll()`(`public/app.js`)의 굴림 중 움직임을 `animateYutThrow()`(v1.6.38)와 같은 스타일로 맞췄다. 순수 시각 보강이며 다른 파일은 건드리지 않았다.

- 굴리는 동안 큐브가 제자리에서 회전만 하던 것을, 윷가락 던지기와 같은 사인파 형태의 상하 튐(`hop`)과 감쇠하는 `rotateZ` 흔들림(`wobbleDecay`)을 더해 더 역동적으로 만들었다. 실제 결과를 결정하는 `rotateX`/`rotateY` 스핀 속도 계산은 그대로 유지했다.
- 멈출 때(`settle()`)는 이전과 동일하게 `DICE_CUBE_ROTATIONS[결과값]`만 대입하는 문자열 전체 교체이므로, 비행 중 추가된 `translateY`/`rotateZ`는 자동으로 초기화된다 — 어떤 눈금이 나올지는 여전히 서버 확정값에만 좌우된다(실제 라이브 굴림으로 "4+5=9" 결과가 화면에도 정확히 4·5로 표시되는 것을 확인).
- 더블 강조, `prefers-reduced-motion` 시 애니메이션 생략 등 기존 동작은 전혀 바뀌지 않았다.

### 버전 동기화 체크리스트 (다음 릴리스에도 동일하게 적용)

버전 문자열은 `package.json`, `server.js`(health 응답 + 시작 로그), `public/index.html`(캐시 버스팅), `lib/release-announcements.js`(새 항목만 — 과거 항목의 버전 텍스트는 절대 건드리지 말 것), 그리고 테스트 내 하드코딩된 버전 정규식(여러 파일에 `app.js?v=1.6.3x` 또는 `health.data.version` 형태로 흩어져 있음, 이스케이프된 `1\\.6\\.3x` 형태도 있으니 sed로 일괄 치환 시 두 패턴 모두 확인)까지 전부 동기화해야 한다.

## 데이터와 배포 주의사항

- Render PostgreSQL: 입장키, 닉네임, 관리자 메모, 공지사항을 영구 저장한다.
- 서버 메모리: 게임방, 진행 중 대국, 세션, 초대, 로비·방 채팅을 보관한다(재배포 시 초기화됨).
- Render 재배포 시 진행 중 게임·채팅·세션은 초기화된다.
- Render PostgreSQL 무료 플랜 만료 예정일은 2026-10-15이다(이전 문서 기준, 이번 세션에서 재확인하지 않음. 만료 임박 시 별도 확인 필요).
- Render 서비스는 `main` 브랜치 커밋마다 자동 배포된다(수동 트리거 불필요, 확인만 하면 됨).

## 작업 규칙

1. 시작 전 `main` 최신 커밋과 이 문서를 확인한다.
2. 아이디어는 `아이디어 저장` 대화(Claude 프로젝트)의 최신 확정사항만 구현한다. **ChatGPT는 이 대화에 직접 접근할 수 없으므로, 사용자가 해당 아이디어를 이 문서나 채팅에 직접 옮겨줘야 구현 가능하다.**
3. 한 릴리스는 한 도구(Claude 또는 ChatGPT)가 끝까지 담당하고, 두 도구가 같은 작업 브랜치를 동시에 수정하지 않는다. 작업 시작 전 어느 도구가 마지막으로 작업했는지 이 문서의 "최신 반영 커밋"으로 확인한다.
4. 기존 게임·저장 데이터·방·관전·재접속·재대결 기능을 보존하고 관계없는 리팩터링·인프라 변경은 하지 않는다.
5. 변경 후 문법 검사, 빌드 가능 여부, `npm test` 전체, 변경 기능 중심 테스트를 실행한다.
6. 사용자에게 보이는 변경은 버전 문자열(위 "버전 동기화 체크리스트") · `lib/release-announcements.js` 공지를 같은 릴리스에서 함께 갱신한다.
7. GitHub `main` 반영 후 Render 자동배포 결과를 확인하고, 이 문서의 "현재 상태"·"릴리스 이력"을 갱신한다.
8. Work 전수 검증은 사용자가 별도로 지시할 때만 한다.

## ChatGPT로 이어받을 때

- ChatGPT는 이 저장소에 대한 GitHub 접근 권한(코드 읽기/커밋 권한이 있는 연동, 또는 로컬 클론)이 별도로 필요하다. Render·GitHub MCP 같은 Claude Code 전용 도구는 없으므로, 배포 확인은 Render 대시보드(`https://dashboard.render.com`)나 `curl https://omok-live.onrender.com/health`로 대체한다.
- 작업 시작 시: 이 문서 전체 → `main`의 `CLAUDE.md`(작업 방식) → 필요한 코드 파일 순으로 읽는다.
- 작업 종료 시: 위 "작업 규칙" 6~7번대로 버전·공지·이 문서를 갱신하고 `main`까지 반영한 뒤 커밋 해시를 이 문서의 "최신 반영 커밋"에 남긴다.

## 저장소 참고

- 핵심 서버: `server.js`
- 화면: `public/index.html`, `public/app.js`, `public/styles.css`
- 게임 로직: `lib/games/`
- 자동 공지: `lib/release-announcements.js`
- 테스트: `test/`
- `README.md`는 초기 버전 설명이라 현재 기능의 기준 문서로 사용하지 않는다.
