# Game Center 인수인계

이 문서는 ChatGPT와 Claude가 교대로 작업할 때의 공통 기준입니다. 대화 기록보다 GitHub `main`과 이 문서를 우선합니다. 세션이 끊기면 다음 작업자는 대화 맥락 없이 이 문서 + `main`의 실제 코드 + `lib/release-announcements.js` 이력만 보고 이어받을 수 있어야 합니다.

## 현재 상태

- 저장소: `dlrjs9702-dot/omok-live`
- 기준 브랜치: `main`
- 현재 릴리스: `v1.6.35`
- 이전 운영 커밋: `940b88c` (v1.6.34)
- 최신 반영 커밋: 이번 릴리스 커밋 해시로 배포 직후 갱신할 것(아래 "최종 반영" 참고)
- 배포 서버(권위 있는 실행 URL): `https://omok-live.onrender.com` — Render 서비스 `omok-live`(`srv-dakk44afngtc73ano4f0`), `main` 브랜치 커밋 시 자동 배포(`autoDeploy: commit`)
- 참고: 과거 문서에 있던 Cloudflare Workers 프록시 주소(`silent-lake-9bcf.dlrjs9702.workers.dev`)는 이번 세션에서 재확인하지 않았다. 접속 안 되면 Render 주소를 기준으로 삼는다.

## 게임과 공통 기능

현재 등록된 게임(코드 기준, `server.js` health 응답의 `games` 목록과 동일): 오목(omok), 오목 2vs2(omok2v2), 사목/커넥트4(connect4), 오델로(othello), 윷놀이(yut), 빙고(bingo), 점과 상자(dots), 랜드킹(cityking), 숫자야구(baseball), 그림 맞히기(pictionary), 라이어게임(liar), 도둑잡기(oldmaid) — 총 12종.

공개·비공개방, 방 비밀번호, 대전 초대, 관전, 재접속, 재대결, 로비·방 채팅, 입장 파일(게스트 키), 관리자 입장 파일 관리·메모·공지사항·접속 현황, 전체 게임 누적 전적 및 1:1 상대 전적 조회를 공통 기능으로 유지한다.

## 릴리스 이력 (v1.6.24 ~ v1.6.35 요약)

전체 공지 문구는 `lib/release-announcements.js`에 릴리스별로 저장되어 있다(코드가 원본, 아래는 목차용 요약).

- v1.6.24 라이어게임 추가
- v1.6.25 도둑잡기(Old Maid) 추가
- v1.6.26 공지사항 버전순 정렬 수정
- v1.6.27 게임방 채팅 높이·스크롤 개선
- v1.6.28 오목 선택 통합, 랜드킹 UI 개선
- v1.6.29 랜드킹 도시 건설, 접속 종료 확인, 윷놀이 경로 수정
- v1.6.30 전체 게임 누적 전적 및 플레이어 조회(영구 저장)
- v1.6.31 랜드킹 더블·전체 턴 계산 수정
- v1.6.32 4자리 숫자야구 진행 불가 버그 수정
- v1.6.33 윷놀이 중앙 경로/빽도/완주 규칙, 1:1 상대 전적, 도둑잡기 UI 다크테마 정리
- v1.6.34 도둑잡기 카드 테이블 UI 및 연출 강화
- **v1.6.35 (현재) 게임방 채팅 전면 개편, 랜드킹 시각화·매각·파산·4인전 확장, 도둑잡기 특수 능력 모드** — 아래 상세

v1.6.23 이전(v1.6.7~v1.6.16 공지 복원 포함)의 세부 이력은 `lib/release-announcements.js`의 git 이력 및 해당 파일 내 각 항목 본문을 참고한다. 이 문서에서는 별도로 중복 기술하지 않는다.

## v1.6.35 확정 범위 (최신 작업 상세)

### 1. 게임방 채팅 전면 개편 (모든 게임 공통)

기존 `room-chat-height.js`가 사이드바 높이를 보드(`.boardCard`) 측정값에 강제로 맞추고 `overflow:hidden`을 걸던 구조가 근본 원인이었다 — 보드가 뷰포트보다 커지면(랜드킹·도둑잡기) `position:sticky` 사이드바가 자기 하단(채팅 입력창)을 화면 밖으로 내보냈다. **이 파일은 삭제했다.** 대신:

1. `.side`의 높이를 `max-height:calc(100vh - 32px)`로 뷰포트에만 종속시켜, 보드가 아무리 커져도 채팅이 절대 화면 밖으로 밀리지 않는다.
2. 사이드바를 채팅/시스템/방정보 3개 탭으로 분리(`public/index.html`의 `#roomSidebar`). 채팅·시스템 메시지 구분은 `lib/room-social.js`에 이미 있던 `type:'chat'|'system'` 필드를 그대로 재사용했다(스키마 변경 없음).
3. 접기/펼치기(`#sideCollapseBtn`), 오버레이 모드(같은 DOM을 `position:fixed`로 재배치 — 별도 채팅 시스템 아님), 플로팅 버튼(`#chatFloatBtn`, 읽지 않은 메시지 배지) 구현. 랜드킹·도둑잡기는 기본적으로 사이드바가 접힌 상태로 시작해 보드 폭을 우선 확보한다(`wideBoardGame()`).
4. PC 전용 3단계 크기 프리셋(좁게/기본/넓게), 모바일은 사이드바를 아예 숨기고 하단 시트(오버레이)로만 접근.
5. 채팅 스크롤: 사용자가 맨 아래에 있을 때만 새 메시지에 자동 스크롤하고, 과거 메시지를 보고 있으면 스크롤을 건드리지 않고 "새 메시지" 버튼만 표시(`renderChat()`의 `wasAtBottom` 판정).
6. `test/chat-height.test.js`를 새 구조에 맞게 전면 재작성.

### 2. 랜드킹(cityking) — 시각화 + 매각 + 파산 + 4인전을 한 번에 재작성

`lib/games/cityking.js`를 **처음부터 다시 작성**했다: 기존에는 플레이어가 하드코딩된 `'black'/'white'` 2인 전용 구조였는데, 이제 다른 다인전 게임(빙고 등)과 똑같이 **좌석 번호(`'1'~'4'`) 기반 2~4인 구조**로 통일했다. `server.js`의 `isNumberedSeatGame`/`seatsFor`에 `isCityKing`을 추가해 기존 좌석·재접속·관전 인프라를 그대로 재사용했다 — 새 좌석 시스템을 따로 만들지 않았다.

1. **시각화**: 칸 종류별 색상+아이콘(출발🚩/도시🏙️/이벤트🎁/세금💰/휴식☕), 소유자 색 상단 띠+테두리, 건물 단계 아이콘(🏠🏡🏢🏨), 좌석별 색상(1=파랑/2=빨강/3=초록/4=보라)의 플레이어 말(겹치지 않게 4분면 배치, 현재 차례 강조), 플레이어 상태 카드(현금·순자산·소유 도시 수·위치·파산 여부), 도시 상세 패널(매입가·통행료·소유자·**다음 건설 비용**·**매각 가능 금액**, 신규 필드). 모두 기존 canvas(`drawCityBoard`)와 DOM(`cityAssets`/`cityTileDetails`) 확장이며 외부 이미지 없이 이모지+CSS만 사용.
2. **매각**: 도시·건물 모두 원가의 50% 고정, 자유 순서(건물만 따로 팔거나 건물 포함 도시 전체를 한 번에 팔 수 있음 — 건물 우선 매각을 강제하지 않음). 내 차례이거나 아래 자산 정리 중일 때만 가능(`sellProperty`/`sellBuilding`, 액션 `sell-property-city`/`sell-building-city`).
3. **파산**: 통행료 등을 낼 현금이 부족하면 즉시 게임을 끝내지 않고 `phase:'liquidate'` 자산 정리 단계로 전환, 자산을 팔아 충당하면 정산 후 정상 진행. 매각해도 부족하고 더 팔 자산이 없으면 파산 확정 — **강제 매각 후 채권자(통행료 수령자)에게 남은 현금을 우선 지급**하고, 해당 좌석은 `eliminated:true`로 표시되어 관전 상태가 되며 턴 순환에서 제외된다(다른 게임을 새로 시작할 필요 없음).
4. **4인전**: 최소 2명, 최대 4명. 마지막 생존자 1명이 남으면 즉시 종료, 아니면 50턴 뒤 생존자를 순자산 내림차순으로 순위 매김(`game.ranking`, 동점은 좌석 순). `game.winner`는 1위 좌석 하나로 기존 코드와 호환.
5. **전적**: `lib/match-result.js`에서 cityking이 3인 이상이면 `game.ranking`을 바탕으로 각 참가자의 `outcomes[].rank`(1위부터)를 함께 기록한다. `lib/match-records.js`의 `validateMatch`/`tally`를 확장해 `rank` 필드를 보존하고 게임별 평균 순위(`avgRank`)를 집계한다 — **기존 승/패/무 저장 스키마는 그대로**(선택 필드 추가일 뿐, 과거 기록과 호환). 3~4인 대국은 기존 규칙대로 정확히 2명 대결이 아니므로 1:1 상대 전적에는 반영되지 않는다.
6. `test/cityking.test.js` 전면 재작성 + `test/new-games-server.test.js`/`test/v1629.test.js`/`test/omok-city-ui.test.js`의 cityking 관련 부분을 좌석 기반 API로 갱신. 게임 선택 화면의 "랜드킹(패치중)" 라벨을 "랜드킹"으로 변경(이번 릴리스로 기능이 실제로 완성됨).

### 3. 도둑잡기 — 특수 능력 모드 (기존 일반 모드·카드 테이블·연출 전부 보존)

`lib/games/oldmaid.js`에 `mode:'normal'|'special'` 필드와 능력 4종을 추가했다(`setMode`/`peekCard`/`redirectTarget`/`armShield`/`detectJoker`). 방장이 시작 전 일반/특수 모드를 고르고(`set-oldmaid-mode` 액션, `#oldmaidModeChooser`), 특수 모드는 시작 시 참가자마다 4종 중 하나를 **무작위**로, 게임당 1회 배정한다.

1. **엿보기**: 내 차례에 뽑기 전, 현재 대상의 카드 한 장의 정체를 나에게만 공개(`game.reveals[seat]` — `publicState()`에는 절대 노출되지 않고 `abilityFor(game, seat)`로만 좌석 본인에게 전달, `handFor()`와 동일한 프라이버시 경계). 대상이 카드를 섞거나 누군가 뽑으면 자동으로 무효화(`clearReveals`).
2. **방향 전환**: 이번 차례만 반대 방향 활성 참가자로 뽑기 대상을 변경. 2인전은 양방향이 같은 상대라 `no-alternate-target`으로 거부.
3. **방어막**: 내 차례가 아니어도 원하는 시점에 발동 가능. 다음번 누군가 내 카드를 뽑을 때 서버가 그 사람이 고른 인덱스 대신 무작위 인덱스를 사용(`draw()` 내부에서 처리) — 뽑기 자체를 막지는 않는다. 발동 즉시 능력 소모, 실제 방어는 트리거 시 소모.
4. **조커 탐지**: 내 차례에 뽑기 대상이 조커를 보유하는지 **여부만**(위치 아님) 나에게 공개.
5. 관전자는 능력을 사용할 수 없고(`findSeat` 없으면 403), 공개 상태(`publicState().abilityUsed`)는 좌석별 "사용했는지" 불리언만 노출 — 어떤 능력인지, 방어막이 걸려 있는지, 엿본 카드가 무엇인지는 전부 좌석 본인에게만(`myOldMaidAbility`) 전달된다.
6. `test/oldmaid-abilities.test.js` 신규(엔진 단위 테스트 + 서버 HTTP 통합 테스트로 프라이버시 경계까지 확인).

### 버전 동기화 체크리스트 (다음 릴리스에도 동일하게 적용)

버전 문자열은 `package.json`, `server.js`(health 응답 + 시작 로그), `public/index.html`(캐시 버스팅), `lib/release-announcements.js`(새 항목만 — 과거 항목의 버전 텍스트는 절대 건드리지 말 것), 그리고 테스트 내 하드코딩된 버전 정규식(여러 파일에 `app.js?v=1.6.3x` 또는 `health.data.version` 형태로 흩어져 있음, 이스케이프된 `1\\.6\\.3x` 형태도 있으니 sed로 일괄 치환 시 두 패턴 모두 확인)까지 전부 동기화해야 한다.

## 데이터와 배포 주의사항

- Render PostgreSQL: 입장키, 닉네임, 관리자 메모, 공지사항을 영구 저장한다.
- 서버 메모리: 게임방, 진행 중 대국, 세션, 초대, 로비·방 채팅을 보관한다(재배포 시 초기화됨).
- Render 재배포 시 진행 중 게임·채팅·세션은 초기화된다.
- Render PostgreSQL 무료 플랜 만료 예정일은 2026-10-15이다(이전 문서 기준, 이번 세션에서 재확인하지 않음. 만료 임박 시 별도 확인 필요).
- Render 서비스는 `main` 브랜치 커밋마다 자동 배포된다(수동 트리거 불필요, 확인만 하면 됨).

## 작업 규칙

1. 시작 전 `main` 최신 커밋과 이 문서를 확인한다.
2. 아이디어는 `아이디어 저장` 대화(Claude 프로젝트)의 최신 확정사항만 구현한다. **ChatGPT는 이 대화에 직접 접근할 수 없으므로, 사용자가 해당 아이디어를 이 문서나 채팅에 직접 옮겨줘야 구현 가능하다.**
3. 한 릴리스는 한 도구(Claude 또는 ChatGPT)가 끝까지 담당하고, 두 도구가 같은 작업 브랜치를 동시에 수정하지 않는다. 작업 시작 전 어느 도구가 마지막으로 작업했는지 이 문서의 "최신 반영 커밋"으로 확인한다.
4. 기존 게임·저장 데이터·방·관전·재접속·재대결 기능을 보존하고 관계없는 리팩터링·인프라 변경은 하지 않는다.
5. 변경 후 문법 검사, 빌드 가능 여부, `npm test` 전체, 변경 기능 중심 테스트를 실행한다.
6. 사용자에게 보이는 변경은 버전 문자열(위 "버전 동기화 체크리스트") · `lib/release-announcements.js` 공지를 같은 릴리스에서 함께 갱신한다.
7. GitHub `main` 반영 후 Render 자동배포 결과를 확인하고, 이 문서의 "현재 상태"·"릴리스 이력"을 갱신한다.
8. Work 전수 검증은 사용자가 별도로 지시할 때만 한다.

## ChatGPT로 이어받을 때

- ChatGPT는 이 저장소에 대한 GitHub 접근 권한(코드 읽기/커밋 권한이 있는 연동, 또는 로컬 클론)이 별도로 필요하다. Render·GitHub MCP 같은 Claude Code 전용 도구는 없으므로, 배포 확인은 Render 대시보드(`https://dashboard.render.com`)나 `curl https://omok-live.onrender.com/health`로 대체한다.
- 작업 시작 시: 이 문서 전체 → `main`의 `CLAUDE.md`(작업 방식) → 필요한 코드 파일 순으로 읽는다.
- 작업 종료 시: 위 "작업 규칙" 6~7번대로 버전·공지·이 문서를 갱신하고 `main`까지 반영한 뒤 커밋 해시를 이 문서의 "최신 반영 커밋"에 남긴다.

## 저장소 참고

- 핵심 서버: `server.js`
- 화면: `public/index.html`, `public/app.js`, `public/styles.css`
- 게임 로직: `lib/games/`
- 자동 공지: `lib/release-announcements.js`
- 테스트: `test/`
- `README.md`는 초기 버전 설명이라 현재 기능의 기준 문서로 사용하지 않는다.
