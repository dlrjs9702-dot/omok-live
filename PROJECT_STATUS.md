# Game Center 인수인계

이 문서는 ChatGPT와 Claude가 교대로 작업할 때의 공통 기준입니다. 대화 기록보다 GitHub `main`과 이 문서를 우선합니다.

## 현재 상태

- 저장소: `dlrjs9702-dot/omok-live`
- 기준 브랜치: `main`
- 현재 릴리스: `v1.6.23`
- 이전 운영 커밋: `ec7102e` (v1.6.22)
- 공개 주소: `https://silent-lake-9bcf.dlrjs9702.workers.dev/`
- 원본 서버: `https://omok-live.onrender.com`

## 게임과 공통 기능

현재 게임은 오목, 오목 2vs2, 사목, 윷놀이, 빙고, 점과 상자, 랜드킹, 오델로, 숫자야구, 그림 맞히기입니다.

공개·비공개방, 방 비밀번호, 대전 초대, 관전, 재접속, 재대결, 로비·방 채팅, 입장 파일, 관리자 입장 파일 관리·메모·공지사항·접속 현황을 공통 기능으로 유지합니다.

## v1.6.7~v1.6.16 공지 이력 보완

`lib/release-announcements.js`의 `v1.6.6`~`v1.6.17` 사이 실제 배포된 버전(관리자 현황판·공개방/초대·오목 2vs2·게스트 재발급·닉네임 변경·사목·공지 고정·승패 연출·윷놀이·점과 상자) 공지 10건이 과거 정리 커밋에서 누락되어 있던 것을 git 로그 기준으로 복원했다. 버전 번호는 변경하지 않았다.

## v1.6.23 확정 범위

1. 게임방 상단의 방 비밀번호·대국 정보를 기본 접힘 상태의 `<details>`로 감싸 필요할 때만 펼치도록 했다. 두 영역은 서로 독립적으로 열고 닫힌다.
2. 신규 게임 그림 맞히기(`pictionary`)를 추가했다. 2~8명, 라운드마다 서버가 정한 제시어를 90초 동안 그림으로 표현하고 나머지가 맞히는 방식이며, 좌표 기반 획 데이터로 실시간 동기화하고 제시어는 출제자에게만 서버가 전송한다.

## v1.6.23 저장 상태

- `roomCodeDisclosure`/`gameInfoDisclosure` 두 `<details>`는 `public/app.js`의 `toggle` 이벤트로 화살표(▶/▼)만 갱신하며, 나머지 접기·펼치기 동작은 네이티브 `<details>`가 담당한다.
- `lib/games/pictionary.js`가 라운드·타이머·점수·출제자 순환을 서버 권위로 관리하고, `server.js`의 1초 주기 타이머(`tickPictionaryRooms`)가 제한시간 종료와 결과 공개 이후 다음 출제자 전환을 자동 진행한다.
- 그림 데이터는 방 상태(`room.game.strokes`)에 보관되어 기존 재접속 구조로 그대로 복구되며, 별도 저장소를 추가하지 않았다.
- 자동 공지 시드에 `v1.6.7`~`v1.6.16` 이력 보완분과 `v1.6.23` 항목을 추가했다.
- 문법 검사, `npm test` 전체 110개 테스트(신규 pictionary 유닛 10개·통합 1개 포함) 통과.
- 로컬 검증은 PostgreSQL 없이 JSON 저장소로 진행했다. Render의 PostgreSQL 연동 자체는 이번 릴리스에서 변경하지 않았다.

## 데이터와 배포 주의사항

- Render PostgreSQL: 입장키, 닉네임, 관리자 메모, 공지사항을 영구 저장한다.
- 서버 메모리: 게임방, 진행 중 대국, 세션, 초대, 로비·방 채팅을 보관한다.
- Render 재배포 시 진행 중 게임·채팅·세션은 초기화된다.
- Cloudflare는 공개 접속 경로이며, 이 저장소 기준 D1·KV·Durable Objects는 사용하지 않는다.
- Render PostgreSQL 무료 플랜 만료 예정일은 2026-10-15이다.

## 작업 규칙

1. 시작 전 `main` 최신 커밋과 이 문서를 확인한다.
2. 아이디어는 `아이디어 저장` 대화의 최신 확정사항만 구현한다.
3. 한 릴리스는 한 도구가 끝까지 담당하고, 두 도구가 같은 작업 브랜치를 동시에 수정하지 않는다.
4. 기존 게임·저장 데이터·방·관전·재접속·재대결 기능을 보존하고 관계없는 리팩터링·인프라 변경은 하지 않는다.
5. 변경 후 문법 검사, 빌드 가능 여부, 변경 기능 중심 테스트만 실행한다.
6. 사용자에게 보이는 변경은 버전·정적 자산 캐시·`lib/release-announcements.js`를 같은 릴리스에서 함께 갱신한다.
7. GitHub `main` 반영 후 Render 자동배포 결과를 확인하고, 이 문서의 현재 상태를 갱신한다.
8. Work 전수 검증은 사용자가 별도로 지시할 때만 한다.

## 저장소 참고

- 핵심 서버: `server.js`
- 화면: `public/index.html`, `public/app.js`, `public/styles.css`
- 게임 로직: `lib/games/`
- 자동 공지: `lib/release-announcements.js`
- 테스트: `test/`
- `README.md`는 초기 버전 설명이라 현재 기능의 기준 문서로 사용하지 않는다.
