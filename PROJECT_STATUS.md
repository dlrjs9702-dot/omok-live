# Game Center 인수인계

이 문서는 ChatGPT와 Claude가 교대로 작업할 때의 공통 기준입니다. 대화 기록보다 GitHub `main`과 이 문서를 우선합니다. 세션이 끊기면 다음 작업자는 대화 맥락 없이 이 문서 + `main`의 실제 코드 + `lib/release-announcements.js` 이력만 보고 이어받을 수 있어야 합니다.

## 현재 상태

- 저장소: `dlrjs9702-dot/omok-live`
- 기준 브랜치: `main`
- 현재 릴리스: `v1.6.57`
- 이전 운영 커밋: `e349b76` (v1.6.56)
- 최신 반영 커밋: `237c068` (v1.6.57, `main` 병합 완료)
- 배포 서버(권위 있는 실행 URL): `https://omok-live.onrender.com` — Render 서비스 `omok-live`(`srv-dakk44afngtc73ano4f0`), `main` 브랜치 커밋 시 자동 배포(`autoDeploy: commit`)
- 참고: 과거 문서에 있던 Cloudflare Workers 프록시 주소(`silent-lake-9bcf.dlrjs9702.workers.dev`)는 이번 세션에서 재확인하지 않았다. 접속 안 되면 Render 주소를 기준으로 삼는다.

## 게임과 공통 기능

현재 등록된 게임(코드 기준, `server.js` health 응답의 `games` 목록과 동일): 오목(omok), 오목 2vs2(omok2v2), 사목/커넥트4(connect4), 오델로(othello), 윷놀이(yut), 빙고(bingo), 점과 상자(dots), 랜드킹(cityking), 숫자야구(baseball), 그림 맞히기(pictionary), 라이어게임(liar), 도둑잡기(oldmaid), 마라톤(marathon) — 총 13종.

공개·비공개방, 방 비밀번호, 대전 초대, 관전, 재접속, 재대결, 로비·방 채팅, 입장 파일(게스트 키), 관리자 입장 파일 관리·메모·공지사항·접속 현황, 전체 게임 누적 전적 및 1:1 상대 전적 조회를 공통 기능으로 유지한다.

## 릴리스 이력 (v1.6.24 ~ v1.6.53 요약)

전체 공지 문구는 `lib/release-announcements.js`에 릴리스별로 저장되어 있다(코드가 원본, 아래는 목차용 요약).

- v1.6.24 라이어게임 추가
- v1.6.25 도둑잡기(Old Maid) 추가
- v1.6.26 공지사항 버전순 정렬 수정
- v1.6.27 게임방 채팅 높이·스크롤 개선
- v1.6.28 오목 선택 통합, 랜드킹 UI 개선
- v1.6.29 랜드킹 도시 건설, 접속 종료 확인, 윷놀이 경로 수정
- v1.6.30 전체 게임 누적 전적 및 플레이어 조회(영구 저장)
- v1.6.31 랜드킹 더블·전체 턴 계산 수정
- v1.6.32 4자리 숫자야구 진행 불가 버그 수정
- v1.6.33 윷놀이 중앙 경로/빽도/완주 규칙, 1:1 상대 전적, 도둑잡기 UI 다크테마 정리
- v1.6.34 도둑잡기 카드 테이블 UI 및 연출 강화
- v1.6.35 게임방 채팅 전면 개편, 랜드킹 시각화·매각·파산·4인전 확장, 도둑잡기 특수 능력 모드
- v1.6.36 채팅·랜드킹·도둑잡기 사용성/안정화 보완, 공통 주사위 굴림 애니메이션
- v1.6.37 랜드킹 게임판 7×11 확장 및 중앙 조작 패널, 3D 큐브 주사위
- v1.6.38 윷놀이 던지기 애니메이션(고급 CSS/JS 연출)
- v1.6.39 랜드킹 주사위 연출을 윷놀이 던지기와 같은 방식으로 보강
- v1.6.40 접속 끊김 공통 처리(1/5) — 모든 게임 공통 일시정지·게임 종료·전적 기록
- v1.6.41 윷놀이 UI 개선(2/5) — 중앙 텍스트 정리, 출발칸 파란색, 이동 버튼 칸수 표기
- v1.6.42 라이어게임 힌트 중 채팅 제한(3/5)
- v1.6.43 도둑잡기 카드 UI·연출 개선(4/5)
- v1.6.44 신규 게임 마라톤 추가(5/5, 지시서 마지막 항목)
- v1.6.45 도둑잡기 뽑기 연출이 짝이 바로 맞는 카드에서 조용히 스킵되던 버그 수정
- v1.6.46 방 채팅을 항상 위에 뜨는 PIP 창으로 보는 선택 기능 추가
- v1.6.47 PIP 범위를 채팅에서 방 전체 화면으로 확장(사용자 의도와 다르게 구현됨, v1.6.48에서 정정) — CSP로 막혀 있던 레이아웃 버그 수정은 유효
- v1.6.48 PIP 범위를 사이드 패널(채팅·시스템·방정보)로 재조정
- v1.6.49 빙고·그림맞히기·라이어게임·도둑잡기·마라톤 중간 포기 추가, PIP 버튼 숨김·스크롤 버그 수정
- v1.6.50 도둑잡기 카드 뽑기 연출이 화면 밖으로 나가던 문제 수정, 원형 테이블 → 반응형 그리드로 개편
- v1.6.51 도둑잡기 "연출 효과" 체크박스가 기기의 동작 줄이기(Reduce Motion) 설정보다 우선하도록 수정
- v1.6.52 도둑잡기 짝 완성 연출을 손패 안에서 자연스럽게 이어지도록 개선
- v1.6.53 도둑잡기 카드 뽑기 연출의 착지 지점을 실제 새 카드 위치로 정확히 맞춤
- v1.6.54 도둑잡기 최대 4인전 + 동서남북 고정 좌석 배치, 카드 연출을 일정 속도 방식으로 전환
- v1.6.55 공통 3D 주사위·윷 애니메이션 시스템, 채팅 위 전용 패널(기본/PiP/접기) 추가
- v1.6.56 전체 게임의 조작 버튼·선택지를 채팅 아래 공용 패널로 재배치
- **v1.6.57 (현재) 윷/주사위 사실적 던지기 물리(다중 바운스+감속 회전), 윷말 서버 경로 기반 단계별 이동 연출, 주사위·윷 패널 PiP를 네이티브 브라우저 창으로 전환** — 아래 상세

v1.6.23 이전(v1.6.7~v1.6.16 공지 복원 포함)의 세부 이력은 `lib/release-announcements.js`의 git 이력 및 해당 파일 내 각 항목 본문을 참고한다. 이 문서에서는 별도로 중복 기술하지 않는다.

## v1.6.57 확정 범위 (최신 작업 상세)

v1.6.56 직후 사용자가 "다음 패치도 계속 진행해"라고 요청했다. 이는 v1.6.56 작업 시작 전 `AskUserQuestion` 2회로 이미 확정해 둔, 다음 패치로 명시적으로 미뤄뒀던 항목이다: PiP 방식을 채팅 사이드바와 동일한 네이티브 브라우저 창(Document Picture-in-Picture)으로 전환하고, "사실적인 3D 물리 애니메이션 + 윷말 단계별 이동" 통합 지시서 전체를 한 패치로 구현하는 것(사용자가 "전체를 한 패치로 구현" 선택).

**구현 범위 (지시서 원문 요구사항 그대로)**:
- **A. 서버 경로 데이터**: `lib/games/yut.js`의 `destination()`(및 내부 `forwardDestination`/`backwardDestination`)이 최종 위치뿐 아니라 실제로 거쳐 간 모든 칸을 순서대로 담은 `path` 배열을 함께 반환하도록 확장했다. `legalMoves()`→`applyMove()`→`publicState().lastMove.destination.path`로 기존 통과 계층 변경 없이 자동으로 흘러간다. 클라이언트가 보기 좋은 경로를 임의로 계산하지 않고, 서버가 이미 판정한 지름길/빽도/센터 경유 규칙이 그대로 반영된 경로만 재생하도록 하기 위함이다(과거 수정된 윷놀이 경로 버그 재발 방지).
- **B. 윷말 단계별 이동 연출**: `animateYutPieceMove(pieceIds, path, opts)`를 새로 추가해, 던지기 애니메이션이 완전히 끝나고 결과가 표시된 뒤에만(`!yutThrowAnimating` 게이트, 별도 상태 머신 없이 기존 `onDone → renderYut()` 재귀 패턴 재사용) 이동 선택이 가능해지도록 했다. 이동이 선택되면 말이 결과 칸으로 순간이동하지 않고 서버가 반환한 `path`를 따라 한 칸씩(칸당 약 400ms 이동 + 100ms 정지) 가볍게 튀어오르며 이동한다. 업힌 말은 함께 이동한다. 캔버스는 DOM 요소 없이 매 프레임 전체를 다시 그리는 방식이라, 모듈 전역 `yutPieceAnimation`(이동 중 피벗 좌표)을 `drawYutBoard()`가 참조해 이동 중인 말만 보간된 위치에 별도로 그리도록 했다(`drawPieceStack()` 헬퍼로 분리, 시각적 동일성 유지). 빠른 보너스 던지기가 연속으로 이어질 때를 대비해 세대 카운터(`yutMoveAnimationGen`)로 새 이동 애니메이션이 이전 것을 조용히 대체하도록 했다. 재접속 시에는 `yutMoveTrackingStarted`(단순 null 체크가 아닌 별도 boolean, 기존 던지기 추적과 동일 패턴)로 이미 끝난 이동을 재생하지 않고 최신 서버 상태만 표시한다.
- **C. 사실적 던지기 물리**: 공용 코어 `animateTumble`을 다중 감쇠 바운스(`bounceHeight()` — 점점 낮고 짧아지는 4단계 바운스)와 감속 회전(`eased = 1-(1-progress)**3`, 등속 회전 후 갑자기 멈추던 것을 마찰로 서서히 느려지는 것으로 교체)으로 다시 작성했다. `animateDiceRoll`/`animateYutThrow` 각각은 요소별 `bouncePhase`/`bounceScale` 난수를 추가해, 윷가락 4개·주사위 여러 개가 서로 다른 타이밍·높이로 튀어 똑같이 움직이지 않도록 했다. 최종 정지 자세(`finalTransforms`)는 여전히 서버 확정 결과로만 결정되므로 연출은 순수 눈요기일 뿐 판정에 영향을 주지 않는다. 주사위 큐브 6면 배치(반대면 합 7)는 이미 구조적으로 올바른 상태였어서 손대지 않았다.
- **D. PiP를 네이티브 브라우저 창으로 전환**: `#diceYutPanel`의 PiP 모드를 기존 `document.body` 재부착 + `position:fixed` 드래그/리사이즈 방식에서, 채팅 사이드바(`openRoomPip`/`closeRoomPip`)와 완전히 동일한 `documentPictureInPicture.requestWindow()` 방식으로 교체했다(`openDiceYutPip`/`closeDiceYutPip`). 실제 패널 DOM을 팝업 창으로 재부착하고, 스타일시트를 팝업 `<head>`에 복제(이 페이지의 CSP `style-src 'self'`가 인라인 `<style>`을 조용히 차단하는 점을 재사용), 레이아웃 오버라이드는 `styles.css`의 `.diceYutPipLayout` 클래스로 처리했다. `default`/`pip`/`collapsed` 세 모드가 서로 배타적인 단일 문자열 구조는 그대로 유지했다. `documentPictureInPicture`를 지원하지 않는 브라우저에서는 PiP 버튼 자체가 숨겨지고 `pip` 모드가 저장돼 있어도 조용히 `default`로 대체된다(사용자 제스처 없이 자동 복원 시도가 실패하는 경우도 동일하게 처리).
- **E. PiP 창 크기에 비례한 3D 오브젝트 축소**: 팝업 창 안에서 `pipWindow.ResizeObserver`로 창 크기를 관찰해(`watchDiceYutPipScale`), `#diceYutStage`에 CSS 커스텀 프로퍼티 `--diceYutScale`을 설정하고 `transform:scale()`로 스테이지 전체를 비례 축소한다. 기존 번역/회전 애니메이션 수식은 전혀 건드리지 않는 순수 CSS 오버레이다.
- **검증**: `npm test` 전체 291개 통과(기존 284개 + 신규 `test/yut-path-regression.test.js` 6개, `test/yut-piece-movement.test.js` 6개 신설 + 기존 `test/yut.test.js`/`test/cityking.test.js`/`test/yut-backdo-finish.test.js` 다수 갱신). 로컬 서버로 라이브 브라우저 검증을 시도했으나, 게스트 입장은 전용 입장 파일(게스트 키) 또는 관리자 비밀번호가 필요해 이번 세션에서 보유한 인증 정보로는 진행할 수 없었다 — CLAUDE.md 6.1의 "이미 통과하는 단위/통합 테스트가 로직을 커버하면 전체 실기 플레이를 재연하지 않는다" 원칙에 따라, 로직 테스트(서버 경로 계산)와 구조적 배선 테스트(DOM/이벤트 연결, 물리 수식, PiP 배선)로 검증을 마쳤다. **채팅 기반 구현은 완료됐으나 실기(라이브 브라우저) 검증은 수행하지 않았다.**

## v1.6.56 확정 범위

v1.6.55 배포 후 사용자가 한 메시지에 여러 요청을 함께 보냈다: (1) 이번 대화 전체를 검토하고 필요한 질문을 정리한 뒤 하나씩 패치할 것, (2) 주사위/윷 PiP를 채팅과 동일한 새 브라우저 창 방식으로 바꿀 것, (3) PiP 창을 키우면 윷/주사위 객체도 창 크기에 비례해 커지게 할 것, (4) "윷말이 몇 칸 움직이는지" 보여주는 버튼이 사라졌다는 버그 제보, (5) 랜드킹 주사위 애니메이션이 안 보인다는 버그 제보, (6) 전체 게임의 선택지·시작 버튼 등 입력 UI를 채팅 아래로 재배치할 것, (7) "사실적인 3D 물리 애니메이션 + 윷말 단계별 이동" 대규모 통합 개발 지시서.

**진단 (질문 전 자체 조사)**: (4)는 실제 버그가 아니라 위치 문제였다 — `#yutMoveChoices` 버튼은 정상 렌더링됐지만 720px 게임판 아래(y≈1155px)에 있어 스크롤해야 보였다. (5)는 로컬 재현에서 재현되지 않았다 — 랜드킹 주사위는 회전 중간 프레임과 서버 확정 결과(예: 4+1)에 정확히 일치하는 정지 자세까지 정상 작동했다. 두 문제 모두 사실상 (6)이 해결책이거나, (7)의 "사실적 물리" 요구가 실제로 원하는 것일 가능성이 높다고 판단해 그대로 진행 여부를 질문에 포함시켰다.

**확정된 사항 (AskUserQuestion 2회)**:
- PiP 방식: 네이티브 브라우저 창(Document Picture-in-Picture, 채팅과 동일 방식)으로 변경 — **다음 패치에서 처리** (이번 패치는 미포함).
- 작업 순서: "B(전체 게임 UI 재배치, 항목 6)" 먼저, "A(사실적 3D 물리 애니메이션 + 윷말 단계별 이동, 항목 2·3·5·7 포함)"는 다음 패치.
- (두 번째 질문 라운드, B의 실제 조사 결과를 바탕으로) 랜드킹의 게임판 캔버스에 겹쳐 그려지던 조작 패널도 이번에 함께 처리, 조작 UI만 이동하고 실제 게임판(그리드·캔버스·트랙)은 그대로 두기, 모바일에서는 조작 UI가 채팅 오버레이를 열어야만 보이는 것을 허용.

**따라서 이번 v1.6.56은 항목 (6)만 구현했고, 항목 (2)(3)(5)(7)은 다음 패치로 명시적으로 미룬 상태다.** 다음 세션은 이 순서대로 "사실적 3D 물리 애니메이션(주사위 실제 굴림·충돌·반동·감속, 윷 막대 입체 모형, 윷말이 실제 경로를 따라 한 칸씩 이동, PiP를 네이티브 브라우저 창으로 전환 + 창 크기에 비례한 3D 오브젝트 크기 조정)"을 통째로 한 패치로 구현하기로 확정돼 있다(사용자가 "전체를 한 패치로 구현" 선택).

- **조사**: Explore 서브에이전트로 전체 게임의 역할선택/시작버튼/조작패널 DOM 구조를 정밀 조사했다. 핵심 발견: (a) `#roleChooser`(흑백·번호 자리 선택)는 이미 모든 게임이 공유하는 단일 컴포넌트라 통째로 옮기면 끝. (b) 시작 버튼은 게임마다 따로(`#bingoStartBtn`, `#cityStartBtn` 등) 있고 각자의 패널 안에 있다. (c) 빙고·그림맞히기·도둑잡기·마라톤은 "게임판"(숫자 그리드/그리기 캔버스/좌석 카드/이동 트랙)과 "조작 버튼"이 한 섹션에 섞여 있다. (d) **`#cityActionPanel`(랜드킹 시작·굴리기·매입·건설 버튼)은 게임판 캔버스 중앙에 `position:absolute`로 겹쳐 그려지는 유일한 예외**였다 — 그대로 옮기면 캔버스에 빈 구멍이 남는다. (e) 랜드킹·도둑잡기는 넓은 보드를 위해 `wideBoardGame()`이 사이드바를 기본 접힘으로 만들고 있어, 조작 버튼을 사이드바로 옮기면 기본값으로 안 보이게 되는 충돌이 있었다.
- **구현**: `<aside id="roomSidebar">` 안, 채팅/시스템/방정보 탭 아래·기권하기 버튼 위에 새 `<section id="gameActionsPanel">`을 추가하고, `#roleChooser` 전체와 각 게임의 시작 버튼·설정 폼·입력창(윷 던지기·이동선택, 빙고 승리조건+시작, 랜드킹 조작 전체, 숫자야구 비밀번호/추측 폼, 그림맞히기 시작+정답폼, 도둑잡기 시작+모드선택, 라이어게임 판수+시작+힌트/추측폼, 마라톤 시작+설정+굴리기+정답폼)을 그 안으로 옮겼다. 빙고 숫자판·도둑잡기 좌석/손패·그림판 캔버스·마라톤 트랙 등 실제 게임판 요소는 원래 자리(게임 화면)에 그대로 남겼다. 모든 요소가 id를 그대로 유지해 `getElementById` 기반 렌더링/이벤트 배선은 손대지 않았다.
  - 랜드킹: `#cityActionPanel`을 사이드바로 옮기고 `position:absolute` 오버레이 CSS를 일반 블록(`display:flex`)으로 바꿨다. `drawCityBoard()`가 비게 된 캔버스 중앙에 "랜드킹" 제목 + 턴/페이즈 요약(`#cityTurnSummary`의 이미 계산된 텍스트 재사용) + 최근 주사위 텍스트를 캔버스로 다시 그려 빈 구멍을 없앴다.
  - `wideBoardGame()` 함수를 삭제하고 `sideShouldCollapse()`가 항상 `false`를 기본값으로 쓰도록 바꿔, 랜드킹·도둑잡기도 다른 게임처럼 사이드바가 기본 펼침 상태로 시작하게 했다(사용자가 수동으로 접는 것은 그대로 가능).
- **회귀 발견 및 수정 (라이브 검증 중)**: 옮긴 요소들은 원래 부모 패널(`#yutControls`, `#bingoPanel` 등)의 `hidden` 토글에 묵시적으로 의존해 "다른 게임일 때 완전히 숨기기"가 됐었는데, 사이드바로 옮기면서 그 안전망이 사라졌다. Playwright로 랜드킹 방에 입장해 확인하던 중 "윷 던지기" 버튼이 랜드킹 화면에도 그대로 남아있는 회귀를 직접 발견했다 — 각 요소의 자체 표시 로직(비활성화 상태, 하위 단계 표시)은 해당 게임의 `renderX()` 함수 안에서만 실행되고 그 함수는 다른 게임일 때 아예 호출되지 않아, 마지막으로 설정된 상태 그대로 남아있었기 때문이다. `renderRoom()`의 중앙 토글 블록에 옮긴 요소마다 `classList.toggle('hidden', !해당게임)`을 명시적으로 추가해 고쳤다(예: `yutThrowBtn.classList.toggle('hidden', !yut)`). 수정 후 Playwright로 omok(패널이 아예 없는 게임)을 재생하며 옮긴 요소 17개가 전부 숨겨지는지, 오목잡기 시작 버튼이 올바로 보이는지 재확인했다.
- **검증**: `npm test` 전체 278개 통과(신규 `test/game-actions-panel.test.js` 8개 포함 — 재배치 위치, 요소별 명시적 hidden 토글 회귀 테스트, `#cityActionPanel` absolute 제거 확인 등). 로컬 서버 + Playwright(관리자 세션 2개를 실제 서버 상태로 만든 뒤 `data-session` 속성을 라우트 인터셉트로 치환하는 방식)로 빙고·랜드킹·윷놀이·도둑잡기·오목(패널 없는 게임)을 각각 라이브로 확인했다: 역할선택/시작버튼/이동선택 버튼이 사이드바 안에 정확히 렌더링됨, 랜드킹 게임판 중앙에 캔버스 상태 텍스트가 빈 구멍 없이 표시됨, 모바일 뷰포트(390px)에서 사이드바가 기본적으로 숨겨져 있다가 채팅 버튼을 누르면 조작 UI까지 함께 펼쳐짐을 스크린샷으로 확인.

## v1.6.55 확정 범위

사용자(지시서)가 "주사위와 윷을 위한 공통 3D 애니메이션 시스템"과 "채팅창 위 전용 애니메이션 패널(기본/PiP/접기)"을 요청했다. CSS 3D transform 기반(실제 물리 엔진·3D 모델링 없이), 애니메이션은 서버가 확정한 결과만 사후에 재생하고 절대 결과를 만들지 않을 것, 기존 게임 로직·공통 시스템은 유지할 것이 조건이었다.

- **공용 애니메이션 코어 추출**: 기존에 `animateDiceRoll`(랜드킹 주사위)과 `animateYutThrow`(윷)가 거의 동일한 모양(회전+튀어오름+감쇠+정착)을 각자 손으로 구현하고 있던 것을, `animateTumble(els, spins, buildFrame, finalTransforms, opts)`라는 공용 rAF 루프로 추출했다. `reducedMotionActive()` 우회, `.settling` 트랜지션, `onDone` 콜백은 공용 코어에 한 번만 존재하고, 두 함수는 각자의 회전 축 순서·최종 변환(주사위: `DICE_CUBE_ROTATIONS` 조회, 윷: `rotateY(0/180deg)`)만 얇은 래퍼로 넘긴다. 수치·순서는 리팩터 전과 동일하게 유지해 시각적 회귀가 없다. 앞으로 추가될 주사위 게임은 `animateDiceRoll(dieEls, finalValues)`를 그대로 재사용하면 된다(이미 N개 주사위·임의 눈금을 지원하도록 일반화돼 있었음).
- **윷 던지기 연출 이전**: 기존 `#yutThrowStage`는 게임판(`#canvasWrap`) 위에 절대 위치로 떠 있다가 던질 때만 약 1.5초간 나타났다 사라지는 오버레이였다. 이를 채팅창(`<aside id="roomSidebar">`) 바로 위에 상주하는 새 패널 `#diceYutPanel`로 옮겼다. 패널은 게임 내내 표시되므로, 더 이상 "던질 때만 보이기" 타이머(`yutStageHideTimer`)가 필요 없다 — 대신 애니메이션 중이 아닐 때는 항상 마지막으로 확정된 결과(뒷면 개수)로 스냅되어 표시된다(`yutLastThrowFlags`로 셔플된 막대 배치를 재사용해, 무관한 리렌더마다 막대가 다시 섞여 보이지 않게 함). 도·개·걸·윷·모·빽도 판정, 말 이동 규칙, 추가 던지기 규칙은 전혀 손대지 않았다.
- **패널 3모드 (기본/PiP/접기)**: 단일 문자열 상태(`diceYutPanelMode`)로 세 모드가 서로 충돌하지 않도록 했다.
  - 기본: `.sideColumn`(채팅 사이드바를 감싸는 새 flex 래퍼) 안에서 `<aside id="roomSidebar">` 바로 위에 도킹.
  - PiP: **네이티브 Document Picture-in-Picture API는 의도적으로 사용하지 않았다** — 지시서가 "브라우저별 지원 차이가 있는 네이티브 PiP API에 의존하지 말라"고 명시했고, 실제로 기존 채팅 사이드바의 "PIP로 보기" 버튼이 바로 그 네이티브 API(Chromium 전용, 실제 두 번째 브라우저 창을 여는 방식)를 쓰고 있어 그대로 재사용할 수 없었다(세션 중 코드 조사로 확인). 대신 패널 DOM을 `document.body`로 재부착하고 `position:fixed`로 띄우는 자체 구현을 했다: 드래그는 헤더에 Pointer Events(`pointerdown`/`pointermove`/`pointerup`), 크기 조절은 CSS 네이티브 `resize:both`(데스크톱만, 모바일 너비에서는 비활성화)를 사용했다. 뷰포트 크기 변경 시 화면 밖으로 벗어나지 않도록 보정한다.
  - 접기: 헤더(제목줄)만 남기고 `.diceYutBody`(3D 스테이지 + 결과 텍스트)를 `display:none`.
  - 모드 전환은 DOM 클래스 토글과 `localStorage` 쓰기만 하며, `state.game`이나 `roomAction`을 절대 건드리지 않는다 — 그래서 모드를 바꿔도 진행 중인 턴이나 서버 상태에 영향이 없고, `#diceYutStage`의 자식 요소(윷 막대)를 다시 만들지 않으므로 애니메이션 rAF 루프가 모드 전환 중에도 끊기지 않는다.
  - 마지막 선택 모드는 `localStorage`에 저장되어 다음 게임에서도 유지된다. 단, 방을 나갈 때(`enterLobby()`)는 PiP로 떠 있던 패널을 화면과 DOM 원래 자리로 되돌리고 숨기되(`resetDiceYutPanelForRoomExit()`), 이 초기화는 `localStorage`의 모드 값 자체는 건드리지 않는다 — 그래서 다음에 윷놀이를 하면 이전에 선택한 모드가 그대로 복원된다.
- **레이아웃 충돌 처리**: 채팅 사이드바 자체의 "접기" 버튼은 기존에 `.gameLayout` 그리드의 2번째 컬럼 전체를 폭 0으로 접었는데(보드 폭을 넓히기 위함), 이제 그 컬럼 안에 주사위·윷 패널도 함께 있어 채팅만 접어도 패널까지 같이 사라지는 문제가 생겼다. `:has()` CSS 선택자로 "패널이 실제로 보이는 상태(숨김도 아니고 PiP로 떠 있지도 않음)"일 때는 컬럼 폭을 원래대로 유지하도록 점진적 개선(progressive enhancement) 규칙을 추가했다 — `:has()` 미지원 브라우저는 기존 동작(채팅 접으면 패널도 같이 접힘)으로 안전하게 대체된다. 모바일에서는 패널이 `<aside>` 내부가 아니라 형제 요소이므로, 채팅이 모바일 오버레이 뒤에 숨어 있어도 패널은 평소 화면 흐름에 그대로 보인다(지시서의 모바일 요구사항).
- **검증**: `npm test` 전체 270개 통과(기존 263개 + 새로 추가/교체한 7개 — 공용 코어 검증, 패널 3모드 검증, 좁은 화면 대응 검증 등). 로컬 서버 + Playwright로 실제 2인 윷놀이 방을 만들어(관리자 로그인 2개 세션을 실제 서버 상태로 생성한 뒤, HTML 응답의 `data-session` 속성을 라우트 인터셉트로 실제 세션 토큰으로 치환해 진짜 로그인 상태를 재현) 다음을 라이브로 확인했다: (1) 윷을 던지면 서버가 반환한 `backs`(뒷면 개수)와 애니메이션이 최종 정착한 막대 뒷면 개수가 정확히 일치, (2) 패널이 게임 진입 시 자동으로 나타남, (3) PiP 버튼을 누르면 실제로 `document.body`로 옮겨져 `position:fixed`로 뜨고, 헤더를 마우스로 드래그하면 실제 위치가 이동함, (4) 기본 모드로 복귀 시 정확히 원래 자리(`.sideColumn`)로 돌아옴, (5) 접기 버튼을 누르면 본문이 `display:none`, (6) 채팅 입력창(`#chatForm`/`#chatInput`)은 패널 상태와 무관하게 항상 정상 작동. 자바스크립트 오류 없음(단, 리팩터 도중 `DICE_CUBE_ROTATIONS`가 중복 선언되어 `Identifier has already been declared` 오류가 발생한 것을 라이브 검증 중 직접 발견해 수정 — 정적 regex 테스트만으로는 잡히지 않았던 문제라 실제 브라우저 로드 확인이 유효했다).

## v1.6.54 확정 범위

v1.6.53 배포 후 사용자가 "훨씬 아래로 가는데?"라고 지적해 확인해보니, 착지 좌표 계산 자체는 수학적으로 정확했다(디버그 로그로 `dx`/`dy`가 실제 도착 요소 중심 오프셋과 정확히 일치함을 확인). 문제는 사용자가 보낸 852×508 같은 짧은 뷰포트에서는 "내 손패"가 화면 맨 아래 바짝 붙어 있어, 정확한 이동이라도 거리 자체가 극단적으로 길어 보였던 것. 사용자가 "브라우저 창 크기에 따라 연출이 달라지는게 좀 아닌 거 같은데... 다른 방식 사용하는게 맞는 거같은데"라며 창 크기에 의존하는 체감 차이 자체를 구조적으로 없애 달라고 요청했다.

- **논의 과정**: 과거 윈도우 솔리테르(카드가 항상 일정한 속도로 움직임)를 참고해 "고정 시간(duration) → 고정 속도(px/ms)" 전환을 제안했고, 이어서 사용자가 "고스톱/세븐포커처럼 중앙에서 카드를 각 자리로 나눠주는 과정이 보였으면 좋겠다", "누구 자리에서 누구에게 가는지 보이게, 최대 인원을 4명으로 줄이면 동서남북 배치가 가능할 것 같다"고 제안했다. 5~6인 지원을 잃는 트레이드오프를 명시적으로 짚고 확인받은 뒤(`1번` 선택), 좌석 배치(나는 항상 남쪽 고정)·손패 위치(기존 하단 sticky 유지)·이동 방식(일정 속도)을 `AskUserQuestion`으로 한 번에 확인받았다. 코드 작성 전 시각적 목업(Artifact)을 먼저 만들어 승인("어 그렇게 설계해")을 받은 뒤 구현했다.
- **인원 축소**: 도둑잡기 최대 인원을 6명 → 4명으로 낮췄다(`lib/games/oldmaid.js`의 `SEATS`·`start()` 상한, `server.js`의 `OLDMAID_SEATS`, `public/app.js`의 `numberedSeats()` 세 곳을 함께 수정). 5~6인 방은 더 이상 만들 수 없다(기존 저장된 방·전적에는 영향 없음).
- **동서남북 고정 배치**: `renderOldMaid()`에서 더 이상 "나"를 좌석 그리드 안에 렌더링하지 않는다 — 나는 항상 화면 하단의 기존 "내 손패" 고정(sticky) 영역으로 표현된다. 상대(최대 3명)만 `opponents.forEach((number, index) => ...)`로 순회하며 `data-compass="north"|"east"|"west"`를 순서대로(북→동→서) 부여한다. CSS도 반응형 `auto-fill` 그리드에서 `grid-template-areas:". north ." "west . east"` 십자 배치로 교체했고(`public/styles.css` `.oldmaidSeats`), 좁은 화면(`max-width:680px`)에서는 세로 1열로 접힌다.
- **일정 속도 카드 연출**: `oldmaidFlyDrawnCard`의 이동 시간을 고정 `.38s` 대신 `거리(px) / 1.5px-per-ms`로 계산하고(220~620ms 범위로 clamp), `transition-duration`을 인라인으로 설정하도록 바꿨다. 동서남북 배치로 좌석-손패 간 거리가 이전보다 훨씬 다양해졌기 때문에(가까운 서쪽 이웃 vs 먼 북쪽 맞은편) 짧은 이동은 굼뜨고 긴 이동은 급해 보이던 고정 시간 방식의 문제를 함께 해결한다. 창 크기와 무관하게 체감 이동 속도가 동일하게 유지된다.
- **검증**: 로컬 서버 + Playwright로 (1) `#oldmaidSeats`가 `display:grid`이고 `grid-template-areas`가 예상대로 적용되는지, (2) `data-compass="north"/"east"/"west"` 요소가 실제로 상단 중앙/우측/좌측에 배치되는지 좌표로 확인했다(합성 DOM에 세 좌석을 임시로 붙여 `getBoundingClientRect()`로 north는 위쪽 가운데, east/west는 그 아래 양옆에 위치함을 확인). `npm test` 전체 263개 통과(`test/oldmaid.test.js` 좌석 수 상한 2개, `test/oldmaid-ui.test.js` 좌석 그리드/`isMe`/`.me` 관련 테스트 갱신, 버전 문자열을 참조하는 다른 게임 테스트 다수의 `1.6.53→1.6.54` 갱신 포함).

## v1.6.53 확정 범위

v1.6.52 배포 후 사용자가 "날아오는 곳이 저 먼 허공인데 내 손패 안에서 생기고 있어. 저 먼 허공으로 날라가니까 시야는 그걸 따라가게 되고 손패 안에 뭐가 새롭게 생기는 걸 인지하기가 힘들어"라고 지적했다.

- **원인**: `oldmaidFlyDrawnCard`가 날아갈 도착 지점을 `oldmaidMyHand.getBoundingClientRect()`, 즉 "내 손패" 컨테이너 전체 박스의 중심으로 계산하고 있었다. 손패 카드들은 `flex-wrap`으로 줄바꿈되며 실제 새 카드는 보통 맨 끝(마지막 줄)에 나타나는데, 컨테이너 전체의 중심점은 카드가 몇 장 없거나 넓게 퍼져 있을 때 실제 카드가 없는 빈 여백 위를 가리킬 수 있어, 사용자 눈에는 카드가 "먼 허공"으로 날아가 사라지는 것처럼 보였다(반면 손패 자체는 이미 v1.6.52에서 실제 카드로 갱신되어 있었지만, 시선이 날아가는 고스트 카드를 따라가느라 그 카드가 이미 손패에 나타난 것을 놓치게 됨).
- **수정**: `oldmaidFlyDrawnCard(originRect, card, destRect)`가 도착 지점을 외부에서 `destRect`로 전달받도록 바꾸고, `oldmaidDrawCard()`에서 `data-card-id`로 방금 뽑힌 카드의 **실제 DOM 요소**를 찾아 그 요소의 `getBoundingClientRect()`를 도착 지점으로 넘기도록 했다(짝이 바로 맞는 경우에도 v1.6.52에서 이미 임시로 손패에 그 카드를 렌더링해두므로 항상 찾을 수 있다). 날아가는 고스트 카드가 이제 정확히 그 새 카드가 있는 자리로 착지한다.
- **검증**: 로컬 서버 + Playwright로 실제 카드를 뽑아, 날아가는 고스트 카드의 착지 위치와 실제 새 카드 요소의 위치 간 거리를 측정했다 — 수정 전에는 수백 px 이상 벗어날 수 있었던 것이, 수정 후에는 카드 한 장 폭 정도(약 52px)로 좁혀졌음을 확인했다. 자바스크립트 오류 없음. `npm test` 전체 263개 통과(`test/oldmaid-ui.test.js`의 관련 테스트 1개 갱신).

## v1.6.52 확정 범위

v1.6.51 배포 후 사용자가 "정상적으로 보이긴 한데, 근데 연출을 뽑은 카드가 상대 패에서 내 손패 안으로 들어온 다음에 같은 것 끼리 빛이 나면서 서서히 사라지는 연출로 해"라고 요청했다.

- **문제**: 기존에는 `roomAction()`이 draw 요청 응답을 받자마자 `state`를 갱신하고 즉시 전체 재렌더링을 하기 때문에, 카드가 즉시 짝이 맞는 경우 "내 손패" DOM은 이미 그 짝이 사라진 최종 상태로 바뀌어 있었다. 그 위에 날아오는 고스트 카드(`oldmaidFlyDrawnCard`)가 도착해서 사라지고, 그와 별개로 손패 밖에 떠 있는 복제 카드(`oldmaidFlyPairsToDiscard`)가 모여서 빛나다 사라지는 식이어서, 사용자 입장에서는 "카드가 들어오는 것"과 "짝이 빛나며 사라지는 것"이 서로 연결되지 않은 두 개의 효과처럼 보였다.
- **수정**: `renderOldMaid()`의 손패 렌더링 부분을 `oldmaidRenderMyHandFaces(hand)`로 분리하고 각 카드 요소에 `data-cardId`를 붙였다. `oldmaidDrawCard()`에서 이번 뽑기가 짝을 완성하는 경우, (1) 먼저 "짝이 제거되기 전" 상태(`[...beforeHand, drawnCard]`)로 손패를 임시로 다시 그려 날아오는 고스트 카드가 실제로 손패 안에 착지하는 것처럼 보이게 하고, (2) 착지 후 `oldmaidGlowAndRemovePair(removedCards)`로 짝이 된 두 장의 **실제 손패 카드**(복제가 아님)에 `data-cardId`로 찾아 빛나는 효과(`pairGlow`)를 준 뒤 페이드아웃(`pairFadeOut`)시키고, (3) 마지막에 진짜 최종 손패(`afterHand`)로 다시 그린다. 기존의 손패 밖 복제 카드 방식(`oldmaidFlyPairsToDiscard`, `.oldmaidPairFlyer`)은 완전히 제거했다. 실제 게임 상태(`state.me.myOldMaidHand`)는 이 과정에서 전혀 건드리지 않는다 — 순수하게 화면 표시만 잠깐 앞서/늦춰 보여주는 것이다.
- **검증**: 로컬 서버 + Playwright로 실제 2인 방에서 카드를 뽑아 짝이 맞는 순간을 재현했다(첫 뽑기에서 바로 두 에이스가 짝을 이룸). 실제 손패 안의 두 카드 요소에 `pairGlow` 클래스가 먼저 붙고, 500ms 후 `pairFadeOut`까지 붙는 것을 확인했고, 손패의 모든 카드 요소가 `data-cardId`를 갖고 있음도 확인했다. 자바스크립트 오류 없음. `npm test` 전체 263개 통과(`test/oldmaid-ui.test.js`의 관련 테스트 1개 교체).

## v1.6.51 확정 범위

v1.6.50 배포 후 사용자가 "어디서 보이는거야? 확인이 안되는데"라고 물었고, 스크린샷·콘솔 확인을 거쳐가며 원인을 좁혀나갔다(순서: 원형 테이블 새 코드가 실제 로드됐는지 확인 → 게임 로직은 정상, 날아가는 연출만 안 보임을 확인 → 로컬에서 조커까지 뽑아 정확히 같은 코드가 정상 작동함을 재현 → 콘솔에서 `window.matchMedia('(prefers-reduced-motion: reduce)').matches` 결과가 `true`임을 확인).

- **원인**: 사용자 기기/브라우저에 "동작 줄이기(Reduce Motion)" 접근성 설정이 켜져 있었다. 기존 `oldmaidEffectsActive()`는 `oldmaidEffectsOn && !oldmaidReducedMotion()`으로, 화면 안의 "연출 효과" 체크박스가 켜져 있어도 시스템 설정이 조용히 덮어써서 모든 연출(날아가는 카드·조커·짝 완성·탈출)을 꺼버리고 있었다. 이 동작 자체는 새로 생긴 버그가 아니라 v1.6.34 시점부터 있던 의도된 접근성 대응이었지만, 사용자가 명시적으로 체크박스를 켜둔 의도를 시스템 설정이 아무 표시 없이 무시한다는 점을 지적받아, **명시적 인앱 선택이 시스템 기본값보다 우선해야 한다**는 방향으로 확정(옵션 2 선택)했다.
- **수정**: `oldmaidEffectsActive()`를 `return oldmaidEffectsOn;`으로 단순화해 체크박스만이 유일한 기준이 되도록 했다(`oldmaidReducedMotion()` 함수 삭제). CSS의 `@media(prefers-reduced-motion:reduce){ .oldmaidFlyingCard{...} ... }` 블록도 함께 제거했다 — 이 블록은 JS가 이미 `oldmaidEffectsActive()`가 거짓일 때 연출 요소 자체를 만들지 않으므로 이전에도 사실상 도달 불가능한 코드였고, 남겨두면 체크박스를 켰을 때도 "날아가는 이동" 대신 "제자리 페이드"로 축소시켜 사용자의 명시적 선택을 방해하게 된다.
- **검증**: `npm test` 전체 263개 통과(`test/oldmaid-ui.test.js`의 관련 테스트 2개 갱신). 별도 라이브 브라우저 검증은 수행하지 않았다 — 이 배포 환경에서는 `omok-live.onrender.com`에 직접 접속할 수 없어(네트워크 정책), 사용자가 직접 실기기에서 재확인해야 한다.

## v1.6.50 확정 범위

사용자가 "여전히 애니메이션이 전혀 안보이고 있는데 도둑잡기"라고 신고했다. "아직 고치진 말고 확인만 해"라는 지시에 따라 먼저 Playwright로 실제 서버를 띄워 재현했고, 원인을 찾아 사용자에게 보고한 뒤 승인을 받고서야 수정했다(이 세션에서도 "확인만 해" 이후 바로 고치지 않고 원인 보고 → 레이아웃 개편 방향 논의(여러 명일 때 배치 질문 포함) → "그 방향으로 진행해" 승인 순서를 지켰다).

- **원인**: 카드 뽑기 시 날아가는 연출(`oldmaidFlyDrawnCard`)은 정상 작동하고 있었지만, 목적지인 "내 손패" 영역이 화면 밖(스크롤해야 보이는 곳)에 있어 사용자 눈에는 "연출이 아예 안 보이는" 것으로 나타났다. 원형 카드 테이블(`.oldmaidTable`, 최대 540px 높이)이 세로 공간을 크게 차지해 그 아래 있는 "내 손패"를 화면 밖으로 밀어냈다. 실제 Playwright 테스트(뷰포트 900px)에서 카드가 도착하는 좌표가 y≈1120으로, 화면 범위를 한참 벗어났음을 확인했다.
- **부수 문제**: 같은 원형 배치 때문에 좌석 하나의 폭이 ~150px로 고정돼 있어, 상대방이 카드를 많이 들고 있으면(예: 6인전 초반 1인당 8~9장) 그 좁은 폭 안에서 가로 스크롤을 해야 카드를 다 볼 수 있었다. 사용자가 이 UX도 불편하다고 지적해 같이 처리했다.
- **해결**: 원형 테이블(`.oldmaidTable`/`.oldmaidFelt`, 좌표 계산 함수 `oldmaidSeatPoint`)을 완전히 제거하고, 참가자를 반응형 CSS 그리드(`grid-template-columns:repeat(auto-fill,minmax(150px,1fr))`, 모바일은 120px)로 배치했다. 각 좌석의 카드 줄(`.oldmaidSeatCards`)은 가로 스크롤(`overflow-x:auto`) 대신 줄바꿈(`flex-wrap:wrap`)되도록 바꿨다. "내 손패" 영역은 `.oldmaidHandDock{position:sticky;bottom:0}`로 화면 하단에 고정해, 인원수·카드 수와 무관하게 스크롤 없이 항상 보이게 했다(자연스럽게 카드 뽑기 연출의 도착 지점도 항상 화면 안에 들어오게 되어 원래 신고된 문제도 함께 해결됨). 고정 시 뒤 콘텐츠가 비치지 않도록 "최근 행동" 섹션을 "내 손패" 앞쪽(문서 순서상 위)으로 옮겼다. 차례/뽑기 대상 강조, 탈출·패배 배지, 짝 완성·조커 긴장 연출 등 나머지 로직·CSS는 그대로 재사용했다(좌석 요소가 `position:absolute`에서 `position:relative`로 바뀐 것 외에는 이 연출들이 기대하는 DOM 구조가 동일).
- **검증**: 로컬 서버 + Playwright로 실제 도둑잡기 방(2인)을 만들어 카드를 뽑아본 결과, (1) `#oldmaidSeats`가 `display:grid`로 렌더링되는지, (2) "내 손패"가 스크롤 없이 뷰포트(900px) 안에 완전히 들어오는지(`top:801,bottom:900`), (3) 날아가는 카드가 애니메이션 전 구간(21틱 관찰) 동안 계속 `inViewport:true`로 유지되는지, (4) 6장을 든 상대 좌석의 카드가 가로 스크롤 없이 2줄로 줄바꿈되는지(스크린샷으로 확인)까지 전부 라이브로 확인했다. `npm test` 전체 263개 통과(`test/oldmaid-ui.test.js`의 원형 테이블 관련 테스트 2개를 그리드/sticky 검증으로 교체, 1개 신규 추가).

## v1.6.49 확정 범위

v1.6.48에서 남겨둔 미해결 항목("빙고 등 5개 게임에 기권 기능이 없는 게 의도된 설계인지")에 대해 사용자가 "의도되지 않은 설계야, 중간에 그만두는 기능이 필요해"라고 확정했다. 이어서 사용자가 "모든 질문을 정리한 후에 패치시작해"라고 요청해 AskUserQuestion으로 설계 결정 두 가지(기존 기권하기 버튼 재사용 여부, 마라톤 팀전 처리 방식)를 한 번에 확인받은 뒤 구현했다. 같은 세션에서 "PIP 안에 기권하기 등 버튼이 안 보인다"는 버그 리포트도 함께 받아 같이 처리했다.

- **중간 포기(기권) 기능 추가**: 빙고·그림 맞히기(pictionary)·라이어게임·도둑잡기·마라톤은 흑/백처럼 뒤집을 진영이 없는 다인 자유경쟁 게임이라 기존 기권 로직이 적용되지 않았다. 새 버튼을 만들지 않고 기존 "기권하기" 버튼·문구를 그대로 재사용(사용자 확인)해, 누르면 그 판이 즉시 `finished`로 종료되고 **그 순간 자리에 남아 있던 나머지 참가자 전원**이 승자로 기록된다. 접속 끊김 시 이미 쓰이던 `end-game`의 "연결된 좌석 전원 승리" 로직과 동일한 패턴을 자가 트리거 버전으로 재사용했다(`server.js` `resign` 액션).
  - 마라톤 팀전은 기권한 사람의 **팀 전체가 패배**로 기록된다(사용자 확인, 접속 끊김 시 팀 전체 패배 규칙과 동일). 남은 팀이 정확히 하나면 접속 끊김 경로와 동일하게 단일 팀 ID(스칼라)로 기록하고, 그렇지 않으면 배열로 기록한다.
  - 빙고·그림 맞히기·라이어게임·도둑잡기는 기권 시 승자를 **항상 배열**로 기록한다(1명만 남아도 배열) — 이 3개 게임(그림맞히기·라이어·도둑잡기)의 기존 자연 승리 조건이 원래도 항상 배열만 만들어내는 것과 모양을 맞추기 위함이다. 빙고만 유일하게 "항상 스칼라"에서 "기권 시 배열 가능"으로 바뀌므로, 빙고의 상태 표시줄·패널 문구에 배열 정규화를 추가했다(도둑잡기·라이어도 기권으로 끝났을 때 `g.loser`/`g.lastResult`가 없는 경우를 위한 대체 문구를 추가했다). `resultOutcome()`(승/패 연출 트리거)도 5개 게임을 배열/스칼라 모두 받아들이도록 통합했고, 이 과정에서 그림 맞히기가 원래부터 `resultOutcome()`에서 아예 처리되지 않던 기존 버그(승패 연출이 뜨지 않음)도 같이 고쳤다.
  - 범위 밖(발견했으나 고치지 않음): 랜드킹(cityking)의 기존 기권 로직에 `(isTeam(room) ? teamColor(seat) : seat) === 'black'` 비교가 좌석 번호(`'1'`)를 문자열 `'black'`과 비교해 항상 거짓이 되는 버그가 있어, 랜드킹은 기권 시 누가 하든 항상 `winner = 'black'`으로 기록된다. 이번 세션 범위(5개 게임)와 무관해 손대지 않았다.
- **PIP 버그 수정 (v1.6.48에서 발견)**: 사이드바를 PIP 창으로 꺼내면 팝업 자체의 좁은 가로폭(400px)이 사이트의 `@media(max-width:880px)` 모바일 브레이크포인트에 걸려, 기권하기·중단된 대국 종료·다음 판 준비 버튼이 들어 있는 `.sideActions` 블록이 통째로 `display:none`이 되어 있었다(모바일은 보드 옆 별도 `.mobileActions` 바를 쓰지만 PIP 팝업엔 보드가 없어 대체 UI가 없었음). `html.sidePipLayout .sideActions{display:grid!important}`로 고쳤다.
- **PIP 내부 스크롤 요청**: "탭·닫기 버튼은 위에, 채팅 입력창은 아래에 고정하고 채팅 메시지만 스크롤되게 할 수 있냐"는 질문에 대응. 기존 도킹된 사이드바가 쓰던 `.side{overflow:hidden}` + `.sidePane{flex:1 1 auto;min-height:0}` + `.chatMessages{overflow-y:auto}` 조합이 그대로 재사용 가능함을 확인하고, PIP 오버라이드가 `.side{height:auto}`(무제한 높이)로 그 조합을 깨고 있던 것을 `.side{height:100%}`(팝업 뷰포트에 고정)로 고쳐 해결했다.
- 테스트: `test/oldmaid-server.test.js`·`test/liar-server.test.js`·`test/pictionary-server.test.js`의 기존 "기권 시 400" 검증을 새 동작(성공 시 200 + 배열 승자, 라이어는 이미 판이 끝난 상태라 409)에 맞게 갱신. `test/bingo-server.test.js`·`test/marathon-server.test.js`(개인전·팀전 각각)·`test/room-pip.test.js`에 새 시나리오 테스트 5개 추가. `npm test` 전체 262개 통과.
- **라이브 검증 미수행**: 이번 릴리스는 Playwright 라이브 브라우저 검증을 시도했으나, 이 앱의 입장 파일(게스트 키)이 동시 1세션 제한을 두고 있어 페이지를 새로 열거나 재로그인하는 우회 경로가 막혀 세션 안에서 완료하지 못했다(UI를 처음부터 끝까지 직접 클릭해서 몰아가는 방식은 이번 패치 범위를 벗어나는 작업량이라 보류). 대신 HTTP 서버 통합 테스트(각 게임의 실제 `resign` 응답과 승자 배열/스칼라 모양을 직접 검증)와 CSS/문자열 수준 회귀 테스트로 대체했다. 추가 라이브 검증은 사용자가 별도로 요청 시 수행한다.

## v1.6.48 확정 범위

v1.6.47 배포 직후 사용자가 "게임창 자체도 pip창으로 빼버리는데 내 말은 그게 아니라 채팅창 및 시스템 창, 방정보 창 등 게임에 필요한 창을 따로 구현해 놓은 부분 그거를 pip모드로 전체 볼 수 있게 하라는 소리였어"라고 정정했다. 이번부터 "다음부터 내가 무엇을 명령하면 정리해서 이해한게 맞는지 확인부터 해"라는 지시가 추가되어, 구현 전 이해한 내용을 사용자에게 먼저 확인받고 승인("맞아 그렇게 구현해") 후 작업했다.

- **범위 재조정**: `#roomView` 전체(보드 포함) 대신 `#roomSidebar`(채팅·시스템·방정보 탭 + 그 안에 이미 있던 기권하기/중단된 대국 종료/다음 판 준비 버튼)만 PIP 창으로 옮기도록 되돌렸다. 보드와 방 상단바는 항상 원래 화면에 남는다. PIP 버튼은 방 상단바에서 다시 채팅 패널의 탭 도구 영역(좁게/기본/넓게 버튼 옆)으로 옮겼다. v1.6.47에서 추가했던 `#roomPipPlaceholder` 안내 카드는 필요 없어져 제거했고, 대신 기존에 있던 "사이드 패널 접기" 레이아웃(보드 전체 너비 확장)을 재사용해 PIP 활성화 중에는 자동으로 보드가 넓어지고 떠다니는 채팅 버튼은 숨겨지도록 했다.
- **CSP 교훈 유지**: v1.6.47에서 발견한 문제(인라인 `<style>` 태그가 이 사이트의 CSP에 의해 조용히 무시됨)의 해결책을 그대로 이어받아, 팝업 레이아웃 오버라이드는 `public/styles.css`의 `.sidePipLayout` 클래스로 유지했다.
- **검증**: 로컬 서버 + Playwright로 (1) 팝업에 채팅·시스템·방정보 탭과 기권/종료/다음 판 준비 버튼이 있지만 보드는 없는지, (2) 메인 화면은 보드가 전체 너비로 넓어지고 채팅 버블이 숨겨지는지, (3) 팝업 안에서 보낸 채팅이 실제로 전송되는지, (4) 팝업 안에서 탭 전환(채팅→시스템)이 정상 동작하는지, (5) computed style로 레이아웃 오버라이드가 실제 적용되는지, (6) 닫으면 사이드바가 원래 자리로 복원되고 보드 레이아웃도 원상복구되는지 전부 확인했다.
- 테스트: `test/room-pip.test.js` 전면 재작성(8개). `npm test` 전체 257개 통과.
- **후속 지시**: 같은 메시지에서 사용자가 "빙고 같은 경우에는 기권하기 버튼 자체가 없는데 다른 게임에도 누락되어 있는지 확인해서 처리해"라고 요청 — 조사 결과 서버(`server.js` `resign` 액션)와 클라이언트(`canResign` 조건)가 정확히 일치하게 빙고·그림맞히기·라이어게임·도둑잡기·마라톤 5개 게임에서 기권을 의도적으로 막고 있음을 확인했다(각 오류 메시지: "OO에서는 기권 기능을 사용하지 않습니다"). 이 5개 게임은 모두 2진법 흑/백 승자 개념이 없는 다인 게임이라, 현재 기권 로직("기권자의 반대 진영을 승자로 기록")이 구조적으로 적용되지 않는다. 코드 누락이 아니라 의도된 설계로 보이며, 접속 끊김 시 사용할 수 있는 공통 종료 기능(`end-game`, 항목 1)은 5개 게임 모두에 이미 적용되어 있다. 이 판단이 맞는지, 5개 게임에 "포기하기" 같은 대체 기능을 새로 설계해서 추가할지는 사용자에게 확인 후 진행 예정(각 게임 규칙마다 접었을 때 처리 방식을 다시 설계해야 하므로 게임당 개별 설계 결정이 필요함).

## v1.6.47 확정 범위

v1.6.46을 배포하고 사용자가 실제로 써본 직후 "기권하게, 종료하게, 다시하기 등이 채팅창 밖에 남아있어서 결국 기존 창도 켜놔야 한다"는 피드백을 받았고, 곧바로 "그냥 원래 창 전체가 pip로 나오게 해줘"로 요구사항이 바뀌어 같은 세션에서 재구현했다.

- **범위 확장**: `#chatSidePane`(채팅 하나)만 옮기던 것을 **`#roomView` 전체**(상단바 포함 — 보드, 채팅, 기권/중단된 대국 종료/다음 판 준비 버튼, 로비로 돌아가기/새 방/접속 종료까지 전부)를 PIP 창으로 옮기는 방식으로 바꿨다. 버튼은 채팅 탭바에서 방 상단바("로비로 돌아가기" 옆)로 옮겼고, 같은 버튼이 PIP 창 안에도 그대로 딸려 들어가 "PIP 닫기"로 표시되며 팝업 안에서 직접 닫을 수 있다. 방이 팝업으로 빠져 있는 동안 원래 화면에는 새로 추가한 `#roomPipPlaceholder` 카드("이 방은 별도 창에서 열려 있습니다" + "이 화면으로 가져오기" 버튼)가 대신 표시된다.
- **라이브 검증 중 실제 버그 발견·수정**: 처음에는 v1.6.46과 같은 방식으로 팝업 안에 `<style>` 태그를 주입해 2단 그리드를 1단 세로 배치로 바꾸려 했는데, 스크린샷에서 여전히 2단으로 보였다. `getComputedStyle`로 직접 확인해보니 실제로 `display:grid`가 그대로 살아 있었다 — 이 사이트의 CSP(`style-src 'self'`, `unsafe-inline` 없음)가 인라인 `<style>` 태그를 조용히 무시하고 있었던 것. 클론한 `<link rel="stylesheet">`(CSP 허용됨)는 정상 적용되는데 인라인 스타일만 막히는 조합이라 화면만 봐서는 원인을 특정하기 어려웠고, computed style을 직접 찍어봐서 확정했다. 해결: 레이아웃 오버라이드를 인라인 태그 대신 `public/styles.css`의 `.roomPipLayout`(팝업 문서의 `<html>`에 붙이는 클래스)로 옮겨 정식 스타일시트를 통해 적용되게 했다.
- **검증**: 로컬 서버 + Playwright로 (1) 팝업이 보드·채팅·기권·로비 버튼을 전부 포함하는지, (2) 팝업 안에서 보낸 채팅이 서버를 거쳐 실제로 전송되는지, (3) 팝업 안 "PIP 닫기" 버튼으로 닫으면 `#roomView`가 원래 자리로 정확히 복원되고 placeholder가 다시 숨겨지는지, (4) CSS 수정 후 `getComputedStyle`으로 `.gameLayout`이 실제로 `display:flex`/세로 배치가 되는지, (5) 스크롤 후 스크린샷으로 채팅 패널이 보드 아래에 실제로 렌더링되는지까지 전부 실제로 확인했다.
- 테스트: `test/chat-pip.test.js`를 `test/room-pip.test.js`로 교체(7개) — 토글 버튼 위치, placeholder, 설정 영속화, `#roomView` 전체 재배치/복원, CSP 세이프 스타일시트 클래스 사용, 방 나가기 시 닫힘. `npm test` 전체 256개 통과.

## v1.6.46 확정 범위

사용자가 "채팅창을 유튜브 PIP 영상처럼 항상 위에 뜨는 독립된 창으로, 위치·크기도 자유롭게 조절하고, 방을 나가면 자동으로 닫히게, 원하는 사람만 켜서 쓰게" 만들어달라고 대화로 요구사항을 단계적으로 확정한 뒤 "그대로 구현해"로 승인한 기능이다. 지시서 항목이 아닌 사용자 직접 요청 기능.

- **구현 방식**: 새 채팅 전용 페이지나 서버 라우트를 따로 만들지 않고, 기존 채팅 패널 DOM(`#chatSidePane` — 메시지 목록·입력폼·전송 버튼을 담은 실제 엘리먼트, 이미 연결된 렌더링·전송 로직 그대로)을 **Document Picture-in-Picture API**(`documentPictureInPicture.requestWindow()`, 유튜브 동영상 PIP와 같은 브라우저 기능)가 열어주는 새 창으로 그대로 옮겨 넣는(reparent) 방식을 썼다. 코드를 복제하지 않았으므로 채팅 로직이 두 군데로 갈라질 위험이 없다.
- **선택 사항·기본값 유지**: 채팅 패널 탭바에 "PIP로 보기" 버튼을 새로 추가했다(크롬·엣지 등 `documentPictureInPicture`를 지원하는 브라우저에서만 보이고, 사파리 등 미지원 브라우저에서는 버튼 자체가 나타나지 않아 기존 방식이 자동으로 유지된다). 기본값은 꺼짐이며, 설정은 `localStorage`(`roomChatPipPref`)에 저장되어 각자 브라우저에서 방을 옮겨도 다시 켤 필요 없이 유지된다.
- **자동 열기/닫기**: 방에 새로 입장(방 만들기·입장·초대 수락)할 때 설정이 켜져 있으면 자동으로 PIP를 열려고 시도한다. 브라우저 보안 정책상 PIP는 사용자의 직접적인 클릭(사용자 제스처) 안에서만 열리므로, 방금 클릭해서 들어온 경우엔 성공하고, 새로고침 후 조용히 재접속되는 경우처럼 클릭이 없던 경우엔 조용히 실패하고 버튼이 "PIP로 보기"(닫힌 상태)로 남아 한 번의 클릭만 더 하면 된다 — 이 제약은 착수 전 사용자에게 미리 설명하고 동의를 받았다. "로비로 돌아가기"(`enterLobby()`)를 부를 때마다 열려 있던 PIP 창을 무조건 닫으며, 같은 방 안에서의 다음 판/재대결은 `enterRoomState()`를 다시 부르지 않으므로 PIP 창이 계속 열려 있는 것이 맞다(방을 나갈 때만 닫힘).
- **라이브 테스트로 실제 동작 확인**: 로컬 서버 + Playwright(Chromium 141)로 실제 PIP 창이 새 브라우저 페이지로 뜨는 것, 메인 문서에서 `#chatSidePane`이 사라지고 PIP 문서 안에 들어가 있는 것, PIP 창 안의 입력창에서 메시지를 보내면 서버를 거쳐 실제로 전송·표시되는 것, 방을 나가면 PIP 창이 자동으로 닫히는 것(페이지 개수 변화로 확인)까지 전부 실제로 확인했다.
- 테스트: `test/chat-pip.test.js`(신규, 6개 — 마크업 id, 기능 감지, 설정 영속화, DOM 재배치/복원, 방 나가기 시 닫힘, 배지 가시성). 기존 회귀 없음 확인 포함 `npm test` 전체 255개 통과.

## v1.6.45 확정 범위

지시서 5개 항목이 모두 끝난 뒤, 사용자가 실제로 v1.6.43에서 배포된 도둑잡기 뽑기 애니메이션을 써보다가 "조커 말고 다른 카드는 작동하지 않고있어"라고 보고한 실사용 버그를 조사해 고쳤다. 지시서 항목이 아니라 v1.6.43에서 들어간 기능의 후속 결함이라 별도 릴리스로 분리했다.

- **근본 원인**: `oldmaidDrawCard()`(클라이언트)가 "무엇을 뽑았는지"를 요청 전/후 손패 diff(`beforeIds`/`afterHand`)로 추론했는데, `draw()`(서버, `lib/games/oldmaid.js`)는 카드를 손패에 넣은 직후 곧바로 `removePairs()`를 호출한다. 즉 **뽑은 카드가 즉시 짝을 완성하면 그 카드는 응답이 나가기도 전에 같은 손패에서 다시 빠진다** — diff의 "이후" 손패에 애초에 존재하지 않으므로 `drawnCard`가 항상 `undefined`가 되어 뽑기 애니메이션(및 짝 제거 연출)이 통째로 스킵됐다. 조커는 규칙상 절대 짝이 맞지 않는 카드라 이 경로를 타지 않아 늘 정상 작동했고, 그래서 "조커만 되는 것처럼" 보였다.
- **수정**: `draw()`가 `removePairs()` 호출 전에 캡처해 둔 카드 참조를 `verdict.card`로 직접 반환하도록 바꿨다(스플라이스는 배열에서 참조를 제거할 뿐 객체 자체는 그대로라 안전). `server.js`의 `draw-oldmaid` 핸들러가 이를 `drawnOldMaidCard`라는 별도 필드로 — 공유되는 `roomView`/`state`에는 절대 섞지 않고, 오직 그 요청을 보낸 뽑은 사람 본인에게만 가는 HTTP 응답에만 — 실어 보낸다. 클라이언트는 `roomAction()`이 이제 응답 데이터를 그대로 반환하도록 한 줄 고친 뒤, `oldmaidDrawCard()`가 더 이상 손패 diff로 추론하지 않고 `data.drawnOldMaidCard`를 직접 사용하도록 바꿨다. 카드 배분, 뽑기 순서, 짝 판정, 조커·특수 능력 규칙은 전혀 바뀌지 않았다.
- **검증**: 로컬에 서버를 띄우고 Playwright로 실제 2인 대국을 진행해 (1) 짝이 바로 맞지 않는 일반 카드, (2) 짝이 바로 맞아 즉시 제거되는 일반 카드(수정 전에는 애니메이션이 뜨지 않던 바로 그 경우) 양쪽 모두에서 카드 뽑기 연출이 정상적으로 화면에 뜨는 것을 스크린샷으로 확인했다. `test/oldmaid.test.js`에 이미 있던 "즉시 짝 완성" 테스트 케이스에 `verdict.card`가 올바른 카드 정체성을 반환하는지 확인하는 단언을 추가했고, `test/oldmaid-server.test.js`에는 `drawnOldMaidCard`가 응답에 포함되고 상대방·관전자 쪽 조회에는 절대 노출되지 않는지 확인하는 단언을 추가했다. `test/oldmaid-ui.test.js`의 낡은 diff-기반 단언 2개를 새 구현에 맞게 갱신했다. `npm test` 전체 249개 통과.

## v1.6.44 확정 범위

"Game Center 통합 패치 작업 지시서" 5개 항목 중 **5번 항목(신규 게임 마라톤)**을 구현해, 지시서의 모든 항목이 완료됐다. 착수 전 사용자가 명시적으로 "먼저 확인해야 한다"고 지정한 5개 미확정 규칙을 모두 AskUserQuestion으로 다시 확인받았다: 시작 위치=0번 칸, 주사위 1개, 30칸 이상 도달 시 즉시 승리, **실패 페널티 이동 후 그 칸에서 새 미션이 바로 이어짐(사용자가 제안 기본값을 뒤집어 명시적으로 확정)**, 팀 배정은 자기가 직접 선택(기존 숫자 좌석 선택 UI 재사용) + 팀원 접속 끊김은 그 팀 전체 패배로 처리(항목 1의 공통 규칙 그대로 적용, 사용자가 재확인).

- **신규 엔진** `lib/games/marathon.js`: 개인전(2~6인, 좌석=그룹)과 팀전(2v2=4인·3v3=6인·2v2v2=6인, 좌석 홀짝/3분할로 팀 자동 배정 — `groupForSeat()`)을 하나의 "그룹" 추상화로 통일했다. 매 착지(주사위 이동이든 페널티 이동이든, 어떤 칸이든 예외 없이)마다 4종 미션(타이핑/기억력/반응/계산) 중 하나를 무작위로 새로 뽑고, 직전과 동일한 문제는 재뽑기로 피한다. 정답은 `publicState()`가 절대 클라이언트에 보내지 않으며 `submitAnswer()`의 성공 응답으로만, 그것도 맞힌 사람에게만 돌아간다. 실패(시간 초과)는 항상 2칸 후퇴 후 그 칸에서 새 미션을 즉시 재발동한다(사용자가 확정한 "연쇄 있음"). 30칸 이상 도달은 미션 없이 즉시 그 그룹의 승리로 게임을 끝낸다. 팀전은 팀원 중 누구나 미션에 답할 수 있고(먼저 맞힌 제출만 인정), 주사위 굴리는 사람은 팀 내에서 매턴 순환한다. 난이도(쉬움/보통/어려움)는 방장이 선택하며 제한시간·기억 길이·계산 난이도에 반영되지만 페널티는 항상 -2칸으로 고정이다(제한시간 구체값은 사용자가 확정하지 않은 항목이라 합리적 기본값을 직접 정했다고 공지·문서에 명시).
- **서버 연동**: `isMarathon`/`marathonSeatSlots()`(팀 구성에 따라 선택 가능한 좌석 수가 4석/6석으로 달라짐)를 `isNumberedSeatGame`/`seatsFor`에 편입해 기존 숫자 좌석 선택 UI·접속 끊김 공통 처리(항목 1)를 그대로 재사용했다. 새 액션 4개(`set-marathon-config`, `start-marathon`, `roll-marathon`, `answer-marathon`)를 라우트 허용 목록과 `handleRoomAction`에 추가했고, 미션 제한시간이 서버 클록으로 자동 만료되도록 `tickMarathonRooms()`(1초 간격, liar와 동일 패턴)를 등록했다. `lib/match-result.js`에 `marathon`을 추가해 팀전의 승패를 좌석이 아니라 그룹 단위로 정확히 집계하도록 했다. **접속 끊김 종료(항목 1의 `end-game`)가 마라톤 팀전에서는 팀 단위로 승패를 매기도록 별도 분기를 추가했다** — 이전 접속-끊김 처리 조건(`isTeam(room)`)이 오목 2vs2 전용이라, 마라톤 팀전은 팀원 중 한 명만 끊겨도 나머지 팀 전원이 (팀 경계를 무시하고) 승리자로 잘못 기록될 뻔한 것을 시작 전에 잡아 수정했다.
- **클라이언트**: 도둑잡기/라이어게임처럼 공용 캔버스 보드를 쓰지 않는 전용 패널(`#marathonPanel`)을 새로 만들었다 — 설정(개인전/팀전/팀구성/난이도) 선택 UI, 30칸 트랙(출발+30칸, 각 칸에 그룹 마커 표시), 주사위 굴리기 버튼, 미션 박스(유형별 안내문·서버 클록 기반 카운트다운·정답 입력 폼 또는 반응형 버튼 4개), 최근 진행 로그로 구성된다. 기억력 미션은 서버가 알려준 `revealMs` 동안만 숫자열을 보여주고 이후 자동으로 가린다.
- **라이브 테스트로 잡은 실제 버그 3건** (자동 테스트만으로는 못 잡았던 것들, 모두 회귀 테스트로 고정했다): (1) 로비의 게임 선택 버튼이 `selectGame()`의 하드코딩된 허용 목록에 `marathon`이 없어 클릭해도 조용히 오목으로 되돌아갔던 문제, (2) 공용 `drawBoard()`의 조기 반환 목록에도 `marathon`이 빠져 있어 마라톤 화면에서 캔버스가 존재하지 않는 오목 보드를 그리려다 매 렌더마다 예외가 나던 문제, (3) `roomView()`가 라이어게임에만 `publicState(game, seat)`처럼 조회자 좌석을 넘기고 마라톤에는 넘기지 않아 `myGroup`/`myTurn`이 항상 비어 있어 "내 차례"인데도 주사위 버튼이 계속 비활성 상태였던 문제. 셋 다 수정 후 실제 2~4인 브라우저 세션으로 개인전·팀전 모두 주사위 굴리기 → 미션(기억력·반응·타이핑 실제 확인) → 정답 제출까지 라이브로 확인했다.
- 테스트: `test/marathon.test.js`(엔진 단위 15개 — 팀 배정, 페널티 연쇄, 30칸 즉시승리, 정답 비공개, 역할 순환 등), `test/marathon-server.test.js`(신규, 실제 서버 기동 — 개인전/팀전 HTTP 흐름, 관전자·상대팀 차단, 접속 끊김 팀 단위 처리), `test/marathon-ui.test.js`(신규, 위 3개 라이브 버그의 회귀 방지), 기존 게임 수 하드코딩 테스트(12→13개) 갱신 포함 `npm test` 전체 249개 통과.

## v1.6.43 확정 범위

"Game Center 통합 패치 작업 지시서" 5개 항목 중 **4번 항목(도둑잡기 카드 UI 및 애니메이션)**만 포함한다. 5번(신규 게임 마라톤)은 v1.6.34/v1.6.35에서 이미 구현된 카드 테이블·뽑기 애니메이션·짝 완성 연출·특수 능력 모드를 그대로 두고, 지시서에서 실제로 남아 있던 부분만 패치했다(중복 구현 없음).

- **카드 뒷면**: `.oldmaidBack`이 `🎴` 이모지 텍스트를 렌더링하던 것을 없애고, `repeating-linear-gradient` 대각선 줄무늬 + 안쪽 테두리 프레임만으로 구성된 순수 CSS 카드 뒷면으로 바꿨다. 기기/브라우저의 이모지 폰트에 의존하지 않으므로 어디서나 동일하게 보인다.
- **카드 확대 · 터치 영역**: 상대방 카드를 데스크톱 `clamp(26,40)→clamp(34,50)px`, 모바일 `clamp(22,32)→clamp(36,46)px`로 키우고, 카드 겹침 폭을 데스크톱 `-15px→-9px`, 모바일 `-12px→-7px`로 줄였다. 기존 마진 값에서는 카드 3~4장만 겹쳐도 이전 카드의 상당 부분이 다음 카드에 가려져 탭이 안 됐는데, 카드가 커지고 겹침이 줄어 각 카드의 클릭 가능 영역이 넓어졌다. 기존 가로 스크롤(`overflow-x:auto`)과 반응형 구조는 그대로 유지했다.
- **뽑기 애니메이션**: 기존에는 클릭 즉시(서버 응답 전에) 카드 뒷면 그림이 날아가는 낙관적 연출이었고, 얼굴은 전혀 보여주지 않았다. `oldmaidDrawCard()`를 다시 짜서 `await roomAction('draw-oldmaid', ...)`가 끝난 뒤 내 손패를 요청 전/후로 비교(`beforeIds`/`afterHand`)해 실제로 새로 들어온 카드를 찾고, 그 카드가 있을 때만(즉 요청이 실제로 성공했을 때만) `oldmaidFlyDrawnCard()`가 그 카드의 실제 무늬·숫자를 얼굴이 보이는 상태로 클릭한 상대 카드 위치에서 내 손패까지 날아가게 했다. 이 요소는 뽑은 사람의 브라우저 안에서만 만들어지므로 다른 참가자·관전자 화면에는 애초에 나타나지 않는다.
- **짝 제거 애니메이션**: 같은 방식으로 요청 전/후 손패 차이를 비교해 방금 제거된 카드(보통 2장)를 찾아 `oldmaidFlyPairsToDiscard()`로 손패 중앙을 향해 모이다가(`translateX`) 노란 글로우(`box-shadow`)가 잠깐 켜지고 줄어들며 사라지는 연출을 추가했다. 다른 참가자가 짝을 완성했을 때는 카드 내용을 알 수 없으므로 기존처럼 좌석 펄스 + "PAIR!" 배지만 표시된다(정보 누출 없음). 두 경우 모두 실제 `state.me.myOldMaidHand` 배열은 건드리지 않는 장식용 복제 요소라서, 서버가 이미 제거한 카드를 클라이언트가 다시 손패에 추가하는 일은 없다.
- **안정성**: 중복 선택 차단(`oldmaidDrawBusy`), 애니메이션 중 중복 요청 차단(모든 `.oldmaidBack` 비활성화)은 기존 그대로 유지된다. 연출은 실패한/오래된 요청에서는 아예 실행되지 않고(손패에 새 카드가 없으므로), 재접속 시 카드 상태는 항상 서버가 다시 보내주는 `state.me.myOldMaidHand`로 복구된다(변경 없음). `oldmaidEffectsActive()`(효과 끄기 토글 + `prefers-reduced-motion`)를 그대로 재사용해 새 연출 두 개 모두 게이팅했다. 조커·특수 능력 규칙은 전혀 건드리지 않았다.
- 테스트: `test/oldmaid-ui.test.js`의 낡은 이모지/낙관적-애니메이션 단언 4개를 새 구현에 맞게 갱신하고, 서버-확정 순서·짝 컨버전스가 실제 손패를 건드리지 않는지 확인하는 테스트 2개를 추가했다. `npm test` 전체 227개 통과. Playwright로 2인 도둑잡기 방을 만들어 카드 뒷면이 이모지 없이 CSS 무늬로 렌더링되는 것과 카드 크기가 커진 것을 스크린샷으로, 실제 뽑기 진행 중 콘솔 오류가 없는 것을 라이브로 확인했다(짝 완성은 카드 분배가 무작위라 이번 확인에서 직접 재현하지는 못했고, 코드 경로는 위 신규 테스트로 검증했다).

## v1.6.42 확정 범위

"Game Center 통합 패치 작업 지시서" 5개 항목 중 **3번 항목(라이어게임 채팅 제한)**만 포함한다. 4~5번(도둑잡기 카드 UI/애니메이션, 신규 게임 마라톤)은 다음 세션에서 이어간다.

- 서버: `/api/room/chat` 핸들러 맨 앞에 `isLiar(room) && room.game.status === 'playing' && ['hint1','hint2','extraHint'].includes(room.game.phase)` 조건을 추가해, 힌트 단계(1차·2차·동률 후보 추가 힌트)에는 어떤 클라이언트로도 우회할 수 없게 `409 LIAR_HINT_CHAT_LOCKED`로 거부한다. 다른 게임과 라이어게임의 나머지 단계(투표·재투표·최종 추측·결과 공개·자리 선택)는 전혀 건드리지 않았다.
- 클라이언트: `chatLockedForHints()`가 `isLiarGame()`으로 게임 종류를 먼저 확인하므로 다른 11개 게임의 채팅에는 영향이 없다. 잠기면 `chatInput`/`chatSendBtn`을 비활성화하고, 채팅창 위에 "힌트 진행 중에는 채팅할 수 없습니다." 안내(`#chatLockNotice`)를 표시한다. `sendChat()`에도 동일 조건의 조기 반환을 추가해 이중으로 막았다. 힌트 단계가 끝나면(투표 등으로 넘어가면) 같은 렌더 사이클에서 자동으로 다시 활성화된다 — 별도 처리 불필요.
- 테스트: `test/liar-server.test.js`(실제 서버 기동 — 힌트 진행 중 채팅 409, 힌트 6회 모두 차단 확인, 투표 단계 진입 후 채팅 200 확인), `test/liar.test.js`(신규, 클라이언트 소스 코드에 잠금 로직·안내문·버튼 비활성화가 존재하는지 검사) 모두 통과. `npm test` 전체 225개 통과. Playwright로 3인 라이어게임 방을 만들어 힌트 단계 진입 → 채팅 입력창/전송 버튼 비활성화 + 안내문구 노출을 라이브로 확인했고, 같은 상태에서 서버 API로 직접 채팅을 시도해 `409 LIAR_HINT_CHAT_LOCKED`가 반환되는 것도 함께 확인했다.

## v1.6.41 확정 범위

"Game Center 통합 패치 작업 지시서" 5개 항목 중 **2번 항목(윷놀이 UI 개선)**만 포함한다. 3~5번(라이어게임 채팅 제한, 도둑잡기 카드 UI/애니메이션, 신규 게임 마라톤)은 다음 세션에서 순서대로 이어간다. 이동·업기·잡기·빽도·완주 등 실제 게임 로직은 전혀 건드리지 않았다(순수 UI 변경).

- 게임판 중앙에 떠 있던 "지름길"(중앙 교차점 위) · "출발 · 완주"(출발 칸 옆) 텍스트 라벨을 제거했다(`drawYutBoard()`). 경로 선·교차점 원은 그대로 유지했고, "완주" 수치는 이미 보드 위 점수 줄(`gameScoreText`: "파랑 완주 N · 빨강 완주 N")과 보드 하단 범례("파랑 집 N · 완주 N")에 남아 있어 정보 손실은 없다.
- 출발 칸(0번 노드)은 다른 칸과 같은 갈색 링 스타일 대신 반지름 30의 꽉 찬 파란색(`#1d4ed8`) 원 + 연노랑 테두리로 그려 한눈에 구분되도록 했다. 파랑·빨강 말이 그 위에 서도 말의 흰 테두리·흰 숫자 덕분에 대비가 유지된다.
- 말 이동 선택 버튼 문구를 도착 칸 번호 중심("1번 말 → 24번 칸")에서 이동 칸수 중심으로 바꿨다: 일반 이동은 "1번 말 · 3칸 이동", 빽도는 "1번 말 · 1칸 뒤로", 업힌 말은 "1·2번 말 · 3칸 이동"처럼 표시하고 완주/완주 직전 칸이면 " · 완주"/" · 완주 직전 칸"을 덧붙인다. `roomAction('move-yut', ...)` 호출과 `legalMoves` 자체는 손대지 않았다.
- 테스트: `test/yut.test.js`에 새 텍스트·출발칸 스타일·제거된 라벨을 확인하는 테스트를 추가했고, 기존 업힌 말 텍스트 검증도 새 포맷에 맞춰 갱신했다. `npm test` 전체 224개 통과. Playwright로 윷놀이 방을 만들어 실제 이동 버튼 문구("1번 말 · 3칸 이동")와 보드 스크린샷(출발 칸이 파란 원으로, 중앙/출발 텍스트 없이 렌더링됨)을 1회 확인했다.

## v1.6.40 확정 범위

사용자가 5개 항목으로 구성된 "Game Center 통합 패치 작업 지시서"를 요청했고, 순서대로 하나씩 구현·검증·배포하기로 했다. 이번 릴리스는 그중 **1번 항목(공통 접속 끊김 처리 및 승패 기록 개선)만** 포함한다. 2~5번(윷놀이 UI, 라이어게임 채팅 제한, 도둑잡기 카드 UI/애니메이션, 신규 게임 마라톤)은 다음 세션에서 순서대로 이어간다.

- 기존에는 오목 2vs2 팀전에만 있던 "일시정지 → 종료" 메커니즘(`syncTeamPause`)을 `syncGamePause`로 일반화해 12개 게임 전체(2인 게임·3인 이상 자유경쟁 게임·4인 팀전 모두)에 적용했다. `assignedSeatsFor(room)`로 게임 유형에 관계없이 배정된 좌석을 구하고, 그중 연결이 끊긴 좌석이 있으면 `game.paused = true`로 표시한다.
- `end-game` 액션을 다시 작성했다: 더 이상 방장 전용·팀 전용이 아니며, **접속 중인 좌석에 앉은 참가자라면 누구나** 일시정지 상태에서 즉시 대국을 종료할 수 있다(관전자는 불가, 일시정지 상태가 아니면 `NOT_PAUSED`로 거부). 승패 기록 규칙은 사용자가 명시적으로 확정한 대로 구현했다: 접속 중인 참가자 전원 승리, 접속이 끊긴 참가자 패배(2vs2 오목 팀전은 팀 단위로 처리 — 팀원 한 명만 끊겨도 그 팀 전체 패배). 3인 이상 자유경쟁 게임(빙고 등)에서 여러 명이 접속 중이면 그 전원이 승리로 기록된다. `game.winner`는 각 게임 엔진이 원래 쓰던 모양(단일 값 vs 배열)을 그대로 따르므로 `buildMatchResult()`는 수정하지 않았다.
- 결과는 `room.recordedMatches`의 기존 라운드 단위 중복 방지 로직을 그대로 타므로 별도 처리 없이 한 번만 기록된다. 새 `game.endReason`(`'disconnect'`)·`game.disconnectedAtEnd` 필드로 "접속 끊김으로 종료된 결과"를 구분해서 표시하며, `prepareNextRound()`에서 두 필드 모두 초기화해 다음 판에 orphan 데이터가 남지 않는다.
- 클라이언트: 모든 게임 공통으로 참가자(관전자 제외)에게 "참가자 OOO님의 연결이 끊겨 게임이 일시 중단되었습니다" 팝업(`<dialog id="pauseDialog">`)을 띄운다. 순간적인 네트워크 지연이 팝업을 깜빡이게 하지 않도록 2초 디바운스를 두었고(그 사이 재접속하면 아예 뜨지 않음), "기다리기"는 그 disconnect 사건 하나만 닫고 "게임 종료" 버튼(`endGameBtn`/`sideEndGameBtn`)은 계속 화면에 남아 있다. 상태 표시줄·보드 오버레이의 일시정지·종료 문구도 팀 전용 조건을 없애고 전체 게임에 적용했다.
- **의도적으로 이번 범위에서 제외한 것**: `move`(오목·오델로·사목·점과 상자·팀전 오목) 액션에는 일시정지 차단을 추가했지만, 그 외 약 15개의 게임별 액션 핸들러(랜드킹 주사위 굴리기, 윷 던지기, 도둑잡기 카드 뽑기, 라이어 힌트/투표/추측, 그림 맞히기 획/정답, 빙고 숫자 선택, 숫자야구 추측 등)에는 일시정지 차단을 개별적으로 추가하지 않았다. 이유: "이 특정 좌석의 접속 끊김이 지금 진행을 실제로 막는가"는 게임마다 판단 기준이 달라 훨씬 큰 작업이 되고, 핵심 요구사항(감지·기다리기/종료 선택·정확한 승패 기록)은 공유 `move` 핸들러 하나만 일반화해도 충족되기 때문이다. 실제로 막힌 게임 진행은 없다 — 접속이 끊긴 좌석이 자기 차례가 아니면 어차피 다른 참가자가 계속 둘 수 있고, 종료 옵션은 모든 게임에서 항상 열려 있다.
- 테스트: `test/connection-drop.test.js`(신규, 2인 게임과 3인 이상 자유경쟁 게임의 실제 서버 기동 통합 테스트), `test/omok-team.test.js`(팀 단위 일시정지·종료 시나리오 갱신), `test/connection-drop-ui.test.js`(신규, 팝업·종료 버튼·문구가 전체 게임 공통으로 존재하는지 소스 검사) 모두 통과. `npm test` 전체 223개 통과. Playwright로 오델로 2인 방에서 실제 연결 끊김 → 팝업 표시(2초 디바운스 확인) → 팝업 안 "게임 종료" 클릭 → 상태 표시줄/보드 오버레이에 "접속 끊김으로 종료" 문구까지 라이브로 1회 확인했다(전수 브라우저 회귀는 수행하지 않음).

## v1.6.39 확정 범위

사용자가 "랜드킹 주사위도 윷 던지기와 같은 방식으로 업데이트해줘"라고 요청해, 공통 `animateDiceRoll()`(`public/app.js`)의 굴림 중 움직임을 `animateYutThrow()`(v1.6.38)와 같은 스타일로 맞췄다. 순수 시각 보강이며 다른 파일은 건드리지 않았다.

- 굴리는 동안 큐브가 제자리에서 회전만 하던 것을, 윷가락 던지기와 같은 사인파 형태의 상하 튐(`hop`)과 감쇠하는 `rotateZ` 흔들림(`wobbleDecay`)을 더해 더 역동적으로 만들었다. 실제 결과를 결정하는 `rotateX`/`rotateY` 스핀 속도 계산은 그대로 유지했다.
- 멈출 때(`settle()`)는 이전과 동일하게 `DICE_CUBE_ROTATIONS[결과값]`만 대입하는 문자열 전체 교체이므로, 비행 중 추가된 `translateY`/`rotateZ`는 자동으로 초기화된다 — 어떤 눈금이 나올지는 여전히 서버 확정값에만 좌우된다(실제 라이브 굴림으로 "4+5=9" 결과가 화면에도 정확히 4·5로 표시되는 것을 확인).
- 더블 강조, `prefers-reduced-motion` 시 애니메이션 생략 등 기존 동작은 전혀 바뀌지 않았다.

### 버전 동기화 체크리스트 (다음 릴리스에도 동일하게 적용)

버전 문자열은 `package.json`, `server.js`(health 응답 + 시작 로그), `public/index.html`(캐시 버스팅), `lib/release-announcements.js`(새 항목만 — 과거 항목의 버전 텍스트는 절대 건드리지 말 것), 그리고 테스트 내 하드코딩된 버전 정규식(여러 파일에 `app.js?v=1.6.3x` 또는 `health.data.version` 형태로 흩어져 있음, 이스케이프된 `1\\.6\\.3x` 형태도 있으니 sed로 일괄 치환 시 두 패턴 모두 확인)까지 전부 동기화해야 한다.

## 데이터와 배포 주의사항

- Render PostgreSQL: 입장키, 닉네임, 관리자 메모, 공지사항을 영구 저장한다.
- 서버 메모리: 게임방, 진행 중 대국, 세션, 초대, 로비·방 채팅을 보관한다(재배포 시 초기화됨).
- Render 재배포 시 진행 중 게임·채팅·세션은 초기화된다.
- Render PostgreSQL 무료 플랜 만료 예정일은 2026-10-15이다(이전 문서 기준, 이번 세션에서 재확인하지 않음. 만료 임박 시 별도 확인 필요).
- Render 서비스는 `main` 브랜치 커밋마다 자동 배포된다(수동 트리거 불필요, 확인만 하면 됨).

## 작업 규칙

1. 시작 전 `main` 최신 커밋과 이 문서를 확인한다.
2. 아이디어는 `아이디어 저장` 대화(Claude 프로젝트)의 최신 확정사항만 구현한다. **ChatGPT는 이 대화에 직접 접근할 수 없으므로, 사용자가 해당 아이디어를 이 문서나 채팅에 직접 옮겨줘야 구현 가능하다.**
3. 한 릴리스는 한 도구(Claude 또는 ChatGPT)가 끝까지 담당하고, 두 도구가 같은 작업 브랜치를 동시에 수정하지 않는다. 작업 시작 전 어느 도구가 마지막으로 작업했는지 이 문서의 "최신 반영 커밋"으로 확인한다.
4. 기존 게임·저장 데이터·방·관전·재접속·재대결 기능을 보존하고 관계없는 리팩터링·인프라 변경은 하지 않는다.
5. 변경 후 문법 검사, 빌드 가능 여부, `npm test` 전체, 변경 기능 중심 테스트를 실행한다.
6. 사용자에게 보이는 변경은 버전 문자열(위 "버전 동기화 체크리스트") · `lib/release-announcements.js` 공지를 같은 릴리스에서 함께 갱신한다.
7. GitHub `main` 반영 후 Render 자동배포 결과를 확인하고, 이 문서의 "현재 상태"·"릴리스 이력"을 갱신한다.
8. Work 전수 검증은 사용자가 별도로 지시할 때만 한다.

## ChatGPT로 이어받을 때

- ChatGPT는 이 저장소에 대한 GitHub 접근 권한(코드 읽기/커밋 권한이 있는 연동, 또는 로컬 클론)이 별도로 필요하다. Render·GitHub MCP 같은 Claude Code 전용 도구는 없으므로, 배포 확인은 Render 대시보드(`https://dashboard.render.com`)나 `curl https://omok-live.onrender.com/health`로 대체한다.
- 작업 시작 시: 이 문서 전체 → `main`의 `CLAUDE.md`(작업 방식) → 필요한 코드 파일 순으로 읽는다.
- 작업 종료 시: 위 "작업 규칙" 6~7번대로 버전·공지·이 문서를 갱신하고 `main`까지 반영한 뒤 커밋 해시를 이 문서의 "최신 반영 커밋"에 남긴다.

## 저장소 참고

- 핵심 서버: `server.js`
- 화면: `public/index.html`, `public/app.js`, `public/styles.css`
- 게임 로직: `lib/games/`
- 자동 공지: `lib/release-announcements.js`
- 테스트: `test/`
- `README.md`는 초기 버전 설명이라 현재 기능의 기준 문서로 사용하지 않는다.
