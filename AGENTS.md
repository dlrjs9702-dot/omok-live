# Game Center 공통 작업 규칙

GPT와 Claude는 이 규칙을 함께 따른다. 도구별 접근 권한과 조작 방법만 각 환경의 지침에 둔다. 이 저장소는 공개이므로 비공개 기록의 본문, 개인정보, 비밀정보를 여기에 옮기지 않는다.

## 1. 정보의 기준 위치

| 정보 | 기준 위치 |
| --- | --- |
| 실제 코드·구현 여부·현재 버전 | `omok-live`의 `main` 및 작업 브랜치 |
| 운영 배포 상태 | Render의 서비스·배포 기록을 그때 조회 |
| 확정 결정·아이디어·우선순위 | 비공개 `dlrjs9702-dot/gamecenter-notes/IDEAS.md` |
| 진행 중 작업·담당·인수인계 | 같은 비공개 저장소의 `STATUS.md` |
| 릴리스별 구현·검증 기술 기록 | 이 저장소의 `PROJECT_STATUS.md` |

다른 곳에는 필요한 링크나 한 줄 색인만 둔다. 자료가 어긋나면 해당 정보의 기준 위치를 확인한다. 기준 자료끼리도 판단이 필요하면 사용자에게 확인한다. 대화 기록이나 오래된 문서만으로 완료·배포를 단정하지 않는다.

## 2. 작업 시작: 필요한 만큼만 읽기

1. 기록 저장소의 `STATUS.md`에서 담당과 진행 중 작업을 확인하고, `IDEAS.md`에서는 이번 작업에 관련된 결정만 읽는다. 비공개 저장소에 접근할 수 없는 환경은 접근 불가를 밝히고, 읽기·기록이 가능한 작업 환경으로 넘긴다. 없는 내용을 추측하지 않는다.
2. 코드 저장소 `main`의 최신 상태와 열려 있는 해당 작업 PR을 확인한다. 이어받는 작업이라면 `STATUS.md`의 브랜치·마지막 원격 SHA를 대조하고 `git log main..<브랜치>`와 `git diff --stat main...<브랜치>`로 범위를 확인한다.
3. 관련 파일·함수와 `PROJECT_STATUS.md`의 해당 릴리스 절만 읽는다. 저장소·대화 전체를 훑거나 같은 내용을 반복 조회하지 않는다. 브랜치나 기록이 예상과 다르면 수정하기 전에 원격과 대조한다.

## 3. 결정과 기록

- `IDEAS.md`의 확정 결정 또는 사용자의 명확한 구현 지시가 있으면 작업한다. 새 지시가 아직 `IDEAS.md`에 없다면 `STATUS.md`에 결정 요약·날짜와 `IDEAS 반영 대기`를 기록하고, 구현과 함께 기록을 반영한다. 사용자 발언 전문을 공개 저장소에 복사하지 않는다.
- 아이디어 저장만 요청받으면 `IDEAS.md`에 상태(제안/확정/보류)와 출처를 기록한다. 구현·배포하지 않는다. 미확정 제안은 확정으로 취급하지 않는다.
- 비공개 기록 저장소는 기록 변경을 `main`에 직접 커밋한다. 직전 파일 SHA를 확인하고 충돌하면 새 내용을 다시 읽어 합친다. 강제 푸시나 무조건 덮어쓰기를 하지 않는다.

## 4. 코드 브랜치·PR·교대

- 코드 저장소 `main`에는 직접 커밋하지 않는다. 코드와 이 저장소의 지침 변경은 작업 브랜치에서 Draft PR로 준비한다. 릴리스 PR 제목은 `[vX.Y.Z] 요약`, 문서만 바꾸는 PR은 `[docs] 요약`으로 쓴다. PR 본문에는 관련 비공개 `STATUS.md` 링크만 두고 민감한 결정 본문을 복사하지 않는다.
- 하나의 작업 브랜치는 `STATUS.md`에 기록된 담당만 수정한다. 의미 있는 단계를 마치거나 중단할 때 원격 작업 브랜치에 커밋·푸시한다. 푸시하지 않은 변경은 인수인계 완료로 취급하지 않는다.
- 정상 교대: 이전 담당이 코드를 푸시하고 멈춤 → `STATUS.md`에 마지막 원격 SHA·완료·남은 일·검증·다음 행동 하나와 `인계대기`를 커밋 → 새 담당이 원격 SHA를 확인 → 담당을 자신으로 바꿔 `STATUS.md`에 커밋 → 코드 수정 시작.
- 갑자기 끊겼다면 마지막 원격 커밋만 확인하고 사용자에게 담당을 확인받는다. 서로 같은 브랜치를 동시에 수정하지 않는다. PR 본문 갱신 기능이 없어도 인수인계는 `STATUS.md`를 기준으로 한다.
- PR 병합과 배포는 해당 사용자 지시의 범위를 따른다. 검토 요청이나 읽기 전용 요청은 병합 승인으로 보지 않는다. 승인 범위가 불명확하면 병합 전 확인한다. `main`에 반영한 뒤 실제 Render 결과를 확인한다.

## 5. 구현·검증·버전·공지

- 요청한 범위에서 작게 완성한다. 로비·방·참가·관전·재접속·채팅·재대결·초대·관리자 기능을 재사용하고, 무관한 리팩터링과 게임 규칙의 무단 변경을 피한다.
- 코드 변경의 기본 검증: 바꾼 JavaScript 파일의 `node --check`, 해당 빌드가 있다면 빌드 확인, `npm test` 전체, 변경 기능을 확인하는 검증 한 가지. 연관 수정은 묶어서 검사하고 같은 검증을 이유 없이 반복하지 않는다. 실행할 수 없는 검증은 결과를 꾸미지 않고 사유를 기록한다. 문서만 바뀌면 링크·변경 범위만 확인하며 코드 테스트나 버전 변경을 요구하지 않는다.
- 사용자에게 보이는 릴리스에서는 `package.json`, `package-lock.json`, `server.js`의 health/시작 로그 버전, `public/index.html`의 캐시 버스팅 값, 테스트의 고정 버전 문자열(이스케이프된 형태 포함)을 동기화한다. `lib/release-announcements.js`에는 해당 릴리스의 새 항목만 추가하고 과거 공지를 임의 수정하지 않는다.
- 공지는 실제 배포할 사용자 표시 변경만 버전·날짜·짧은 목록으로 쓴다. 저장한 아이디어, 미완성 변경, 실패한 시도는 공지하지 않는다. 해당 릴리스의 기술 기록은 병합할 PR 안에서 `PROJECT_STATUS.md`에 추가한다.
- Work 환경의 전체 실기·전수 검증은 사용자가 별도로 지시할 때만 한다. 자동 테스트와 실제 브라우저·모바일 검증을 혼동하지 않는다.

## 6. 배포·보고·안전

- Render에서는 배포 상태를 직접 조회한다. 게임방·대국·세션·채팅은 서버 메모리에 있어 재배포 시 초기화될 수 있다. 문서만 바꾼 PR의 배포를 건너뛸 때는 **실제 `main` 병합 커밋 메시지**에 `[skip render]`를 넣고 Render Events에 건너뛰기 기록이 남았는지 확인한다. 코드 변경과 묶어 배포를 건너뛰지 않는다.
- 보고에는 `코드 준비 / GitHub 반영 / 배포 시작 / 배포 완료 / 실기 미검증 / 실기 검증`을 사실에 맞게 구분한다. 비밀정보를 노출하거나 파괴적 DB 변경, 범위 밖 파일 삭제, 임시 워크플로 방치를 하지 않는다.
